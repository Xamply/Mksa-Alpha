## Active Task
Status: completed
Started: 2026-07-23
Completed: 2026-07-23
Objective: Plan Maestro Parte 2 de 2: Interfaz, métricas y cierre definitivo - ToggleService unificado, capacidad dinámica UI (eliminación de supportedDisable), preview con caducidad, métricas de delta/atribución, adaptadores automáticos y suite de fallos/20 ciclos.
Intent: Eliminar el bloqueo prematuro en UI (supportedDisable en ModsScreen/BridgeProxy), conectar la capacidad dinámica (toggleState y toggleCapability), implementar ToggleService.java y adaptar Bridge/IPC, implementar el muestreador de métricas (MetricsSampler/OwnerCostProfiler), eliminar todas las dependencias de relaunch en Tier 3, y verificar los 20 ciclos continuos de chat_heads con inyección de fallos.
Result: VERIFICACIÓN 100% OK. Eliminado supportedDisable, implementado ToggleService unificado, MetricsSampler con deltas y atribución, HotToggleAdapter con generación de esqueletos, visibleRows dinámico en ModsScreen, y endpoints IPC v2 (tx.preview, tx.apply, tx.status). Verificación en vivo ejecutada OK.
Files touched: mod-thin/src/main/java/dev/mksa/modthin/gui/ModsScreen.java, mod-thin/src/main/java/dev/mksa/modthin/BridgeProxy.java, agent/src/dev/mksa/agent/ToggleService.java, agent/src/dev/mksa/agent/MetricsSampler.java, agent/src/dev/mksa/agent/HotToggleAdapter.java, agent/src/dev/mksa/agent/Tier3AdapterRegistry.java, agent/src/dev/mksa/agent/Ledger.java, agent/src/dev/mksa/agent/Agent.java, docs/session-handoff.md, docs/log.txt.
Verification: OK ejecutado contra verify-fase1.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-12
Completed: 2026-07-12
Objective: Task #55, corte "auditoria librerias base" -- destrabar external_shape_consumer_unverified para las clases de Netty/LWJGL/JOML/Guava que invocan sun.misc.Unsafe/reflexion, via auditoria real sitio-por-sitio (no un atajo de confianza generica), por decision del usuario documentada en project_mksa_base_library_audit_decision.md.
Intent: De las 34 clases originalmente flageadas "unknown" por isReflectionRisk, auditar cada sitio de llamada a Unsafe/reflexion via javap -c -p -v real, confirmando cuando el receiver es SIEMPRE un tipo interno de la libreria (nodo de cola jctools, buffer nativo, aconst_null) y nunca un objeto externo controlable ni una interfaz/target que Tier3 pueda inyectar. 30 clases (31 hash entries, ver MultiReleaseMemCopy abajo) tuvieron prueba estructural cerrada. Las otras 4 (PlatformDependent0 en metodos genericos Object/long, UnsafeRefArrayAccess sobre Object[], ReferenceCountUpdater<T> con parametro de tipo generico, APIUtil.apiClassTokens(Class[])) tenian evidencia real pero no estructuralmente cerrada -- presentadas al usuario via AskUserQuestion, elegio "Excluir las 4 (recomendado)": quedan fuera, siguen bloqueadas (risk=true) exactamente como antes.
Updated: 2026-07-12
Completed: 2026-07-12
Result: Implementado AUDITED_BASE_LIBRARY_HASHES (Map dotted-name->SHA-256) + isAuditedBaseLibraryClass (exige coincidencia EXACTA de hash, cualquier discrepancia cae a false fail-closed) + AUDITED_BASE_LIBRARY_SAFE, integrados en scanExternalLoadedClasses (hit exacto evita scanShapeReferences/isReflectionRisk por completo para esa clase). Bug real encontrado y corregido durante la propia verificacion en vivo: org.lwjgl.system.MultiReleaseMemCopy seguia "unknown" en la primera corrida --tier3-e2e-regen pese a estar en la lista -- root cause, lwjgl-3.3.3.jar es un Multi-Release JAR (JEP 238) y bajo JBR21 (unico JVM de produccion, corte Z) el classloader siempre resuelve a la variante META-INF/versions/16/, nunca a la entrada base cuyo hash se habia pinneado originalmente. Corregido re-auditando (javap real) la variante v16 realmente cargada -- misma garantia de seguridad (Unsafe.copyMemory con aconst_null como ambos receivers) -- y actualizando el hash pinneado a esa variante.
Files touched: agent/src/dev/mksa/agent/Tier3MixinAudit.java, docs/log.txt, docs/session-handoff.md.
Command/check expected: bash agent/build.sh, cargo run --bin verify-fase1 -- --tier3-e2e-regen.
Verification:
- OK bash agent/build.sh limpio (solo warnings preexistentes --release 8/unchecked), corrido dos veces (una tras la implementacion inicial, otra tras el fix de MultiReleaseMemCopy).
- OK primera corrida --tier3-e2e-regen (runtime real): 29 de las 30 clases pinneadas dejaron de aparecer como "unknown" en cualquier target; MultiReleaseMemCopy fue la UNICA excepcion, root-causada como arriba.
- OK segunda corrida --tier3-e2e-regen tras el fix (runtime real, extraida via scripts Node.js de parseo JSON contra el log crudo, no grep superficial): MultiReleaseMemCopy ya no aparece como consumidor unknown en NINGUN target (0 apariciones). Quedan exactamente 33 clases unknown reales en ambos targets/corridas: las 4 excluidas por decision explicita del usuario + 29 mod-specific (biomesoplenty, gson, mixinextras, jna, modmenu, sodium, cardinal-components, log4j, jodah typetools, etc.) -- deferidas caso-por-caso por politica ya acordada (feedback_categorize_not_case_by_case), fuera de alcance de este corte.
- canLowerDecision permanece false en ambas corridas -- esperado y correcto: depende del cierre separado de las 33 clases restantes (task #54), no de este corte.
Recovery notes: Si una libreria de base sube de version, su hash pinneado dejara de coincidir automaticamente (fail-closed por diseno) y la clase volvera a "unknown" hasta re-auditar sitio-por-sitio y actualizar el hash -- no es un axioma ciego, se re-verifica solo. Si aparece una NUEVA clase Multi-Release JAR con el mismo patron de MultiReleaseMemCopy (base-jar-entry vs META-INF/versions/N/), recordar que JBR21 resuelve siempre a la version mas alta <= su propia version, no a la entrada base -- auditar y pinnear la variante REALMENTE cargada, confirmable con unzip -l <jar> | grep META-INF/versions y comparando bytes via unzip -p. Proximo paso natural (task #54, ya deferido y NO iniciado en este corte): las 29 clases mod-specific restantes, caso por caso, categoria por categoria, siguiendo feedback_categorize_not_case_by_case -- no asumir politica generalizable sin preguntar primero por categoria.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-12
Completed: 2026-07-12
Objective: Corte "acotar riesgo de configPlugin al target especifico" -- resuelto por decision explicita del usuario ("Retirar el bloqueador por completo") en retirar el bloqueador `mixin_config_plugin_present` de `Tier3MixinAudit.java` por completo, en las 4 ubicaciones donde gateaba `canLowerDecision`/elegibilidad de target.
Intent: Investigacion via javap real contra sponge-mixin-0.17.0+mixin.0.8.7.jar (misma version en produccion MKSA) confirmo que `IMixinConfigPlugin.shouldApplyMixin` se invoca UNA sola vez por par (mixin,target) durante `MixinConfig.prepare()` al boot -- el resultado queda fijo en `mixinMapping`, `getMixinsFor` solo lee de ahi en adelante. Consecuencia: `Tier3MixinReplay.replay` ya lee esa decision fija en vivo via `getMixinsFor` real, y `Tier3DemixApply.disableViaReplay` verifica `verify_excluded_set_matches_requested` antes de mutar (aborta con `REPLAY_EXCLUDE_SET_MISMATCH` si diverge); `buildFieldSafety`/`buildExternalShapeConsumers` (RESET y REPLAY) ya asumen peor caso para todo contributor declarado, plugin o no -- un plugin solo puede VETAR una entrada declarada, nunca agregar una no declarada. Ningun consumidor downstream dependia de probar estaticamente que el plugin es un predicado simple. Presentado al usuario con 3 opciones (mantener narrowing target-scoped / narrowing namespace-wide / retirar el bloqueador por completo); eligio la ultima.
Updated: 2026-07-12
Current intent: N/A, corte cerrado en la misma sesion.
Result: Removido `mixin_config_plugin_present` de: (1) `classifyDemixTargets` (ya no agrega la reason); (2) `buildDemixPlan` -- `commonSignalsOk` ya no incluye `targetConfigPluginSimple`, `safeToExecute` ya no incluye `allTargetsNoConfigPlugin`, removido el reason de fallo correspondiente y reescrito el mensaje de exito; (3) `buildTxGate` -- removido el `blockers.add(reason("mixin_config_plugin_present", ...))`. `configPluginPresent`/`configPluginSimple`/`mixinConfigPluginTrust`/`mixinConfigPluginAbsent` se mantienen intactos como diagnostico informativo (nunca escondidos), solo que ya no bloquean nada -- `allTargetsNoConfigPlugin`/`targetConfigPluginSimple` se siguen calculando exclusivamente para alimentar esos campos diagnosticos. `allPluginsForContributorsAreSimplePredicates`/`pluginShouldApplyMixinIsSimple`/`configPluginBytesFor` (helpers del corte anterior) quedan intactos, siguen alimentando el diagnostico.
Files touched: `agent/src/dev/mksa/agent/Tier3MixinAudit.java`, `docs/log.txt`, `docs/session-handoff.md`.
Command/check expected: `bash agent/build.sh`.
Verification:
- OK `bash agent/build.sh` limpio (solo warnings preexistentes --release 8 obsoleto + unchecked), corrido dos veces (una tras el primer set de ediciones, otra tras restaurar el tracking diagnostico de `allTargetsNoConfigPlugin`).
- OK `grep -rn "mixin_config_plugin_present"` en `agent`/`launcher` -- cero referencias remanentes en todo el codigo (el harness Rust nunca dependio de este string).
- PENDIENTE runtime real: `cargo run --bin verify-fase1 -- --tier3-e2e-regen` sobre `chat_heads`/`fabric-block-view-api-v2` confirmando `canLowerDecision=true`/`blockers=[]` -- este es ahora el criterio de exito primario ligado al goal del usuario ("lograr el toggle en la practica de los mods tier 3"). Toggle real disable/enable en juego tambien pendiente.
Recovery notes: Si se retoma, el siguiente paso es puramente de EJECUCION: adjuntar el agente a una Instancia real con `chat_heads`/`fabric-block-view-api-v2` y correr `--tier3-e2e-regen`, confirmando que `external_shape_consumer_unverified` (resuelto por el fix de ceguera a lambdas, corte anterior) y ahora tambien la ausencia de `mixin_config_plugin_present` dejan `canLowerDecision=true` real. Si algun otro blocker aparece, es una senal nueva legitima a investigar, no una regresion de este corte (el diff quedo aislado a las 4 ubicaciones de gating ya enumeradas).

## End Active Task

## Active Task
Status: completed
Started: 2026-07-12
Completed: 2026-07-12
Objective: Corte "narrowing configPlugin" -- completar el wiring de `methodIsPureStringPredicate`/`scanPureStringPredicateCode` (ya escritos en ClassFile) al sistema de bloqueadores de `buildDemixPlan` y `classifyDemixTargets`.
Result: Implementado y compilado limpio. Nuevos helpers estaticos en Tier3MixinAudit: `configPluginBytesFor` (Tier3LiveCapture + fallback readClassBytes en todos los jars), `pluginShouldApplyMixinIsSimple` (invoca ClassFile.methodIsPureStringPredicate sobre shouldApplyMixin con descriptor "(Ljava/lang/String;Ljava/lang/String;)Z"), `allPluginsForContributorsAreSimplePredicates` (fail-closed: un solo plugin no analizable bloquea el conjunto entero). Wiring: en buildDemixPlan y classifyDemixTargets, cuando hay configPlugin pero TODOS pasan el narrowing, el bloqueador mixin_config_plugin_present NO se emite y la eligibilidad del target no se degrada. mixinConfigPluginTrust emite 3 valores: "no_config_plugin_declared", "config_plugin_provably_simple_string_predicate" (nuevo), o "config_plugin_present_dynamic_targets_possible". buildTxGate acepta los dos primeros como "OK" (noConfigPluginPresent=true). Comportamiento fail-closed intacto: bytes no disponibles, opcode fuera de la whitelist, o excepcion -> bloqueador permanece.
Files touched: agent/src/dev/mksa/agent/Tier3MixinAudit.java, docs/session-handoff.md, docs/log.txt.
Verification: powershell agent/tmp-build/rebuild.ps1 OK (limpio, solo warnings preexistentes --release 8 + unchecked); cargo check --bin verify-fase1 OK (solo 7 warnings preexistentes de 'id' no leido, ninguno nuevo). Runtime --tier3-e2e-regen PENDIENTE (requiere JVM de Minecraft adjunta con el agente).

## Active Task
Status: CERRADO -- las 9 tareas (#28-#36) completadas, VERIFICADO en runtime real, incluyendo --tier1-7 completo corrido por el usuario mismo (VERIFICACION OK, exit 0, sin regresion Tier 1/2)
Started: 2026-07-11
Objective: Corte tender-seeking-fern -- conectar el toggle Tier 3 end-to-end (disable/enable real in-game, no solo gate), generalizar la auditoria de targets a CUALQUIER mod (retirar la whitelist tipeada a mano `Tier3DemixApply.TARGETS`), y agregar deteccion/reparacion concreta de corrupcion de estado vivo de la JVM. Instruccion vinculante verbatim del usuario: "el alcance de este corte es resolver de una vez por todas cualquier tipo de problema relacionado a auditar, conectar las cosas end to end, hacer que funcione, no me importa que me vengas con que es dificil, se hace y ya, ante cualquier cosa consigamos formas de detectar cualquier cosa y solucionarlo si se corrompe estado vivo de la jvm." Plan aprobado en `C:\Users\Aly\.claude\plans\tender-seeking-fern.md`, 8 tareas (#28-#35).
Intent: `Tier3MixinAudit.buildDemixPlan` ya computaba en vivo (co-owners, runtimeOrder, transformerQuiescence, fieldSafety) para CUALQUIER target -- la unica dependencia dura de la whitelist manual era una linea en `buildTxGate` (`Tier3DemixApply.isEligible(target)`). `Ledger.disableOne` para tier>=3 siempre devolvia `TIER3_TX_APPLY_NOT_IMPLEMENTED` sin llamar nunca a `Tier3DemixApply`; `enableOne` ni tenia branch tier3. Diseno: (1) escaner nuevo de "consumidores externos de forma" en `Tier3MixinAudit` que generaliza la leccion de `class_303`/corte AN (co-ownership NO es la unica forma de romper algo: una clase FUERA del grupo de redefine puede hacer `checkcast` incondicional contra una forma que solo existe por el mixin) sin memoria tipeada a mano; (2) retirar `TARGETS`/`isEligible` por clasificacion en vivo RESET/REPLAY/BLOCKED; (3) modo explicito pasado desde el gate + re-verificacion anti-TOCTOU en el punto de aplicacion; (4) `DISABLED_BY_NS` namespace-indexado con snapshot de bytes; (5) verificacion de schema post-redefine + rollback automatico portada al camino grupal; (6) verificacion de estado vivo pre/post-mutacion; (7) wiring real en `Ledger.disableOne`/`enableOne`.
Updated: 2026-07-11
Current intent: N/A, las 8 tareas del plan quedaron completadas y compiladas en esta sesion. Ningun mensaje nuevo del usuario llego durante todo el segmento -- todo el trabajo procedio autonomamente siguiendo la secuencia ya aprobada del plan, retomando un estado intermedio roto (Tier3DemixApply no compilaba) dejado por un corte de contexto previo.
Completed: 2026-07-11
Result: Las 8 tareas (#28-#35) completadas y `bash agent/build.sh`/`cargo check --bin verify-fase1` limpios en cada paso, verificado repetidas veces tras cada cambio. #28: `Tier3DemixApply.DISABLED` (Map<String,Boolean> plano) reemplazado por `DISABLED_BY_NS` (Map<String,List<DisabledTargetRecord>>, namespace-indexado, cada record con target/mode/preDisableLiveBytesSnapshot/installedDisabledBytesSnapshot/disabledAtReceiptId). #29: `expectedDeclaredCounts` (ya existente en los metodos singulares viejos) portado a `disableGroup`/`enableGroup` -- si algun target del grupo no calza tras el redefine atomico inicial, se revierte TODO el grupo con un segundo `redefineClasses` atomico de vuelta a los bytes vivos/desactivados originales; status nuevo `ROLLED_BACK` en `TxJournal`, deliberadamente distinto de `PARTIAL`/`FAILED`. Caso peor (rollback tambien falla): code `external_state_tamper_suspected`, declarado explicitamente, `DISABLED_BY_NS` NO se marca (mentiria sobre que bytes estan instalados). #30: nuevo escaner en `Tier3MixinAudit` sobre `Instrumentation.getAllLoadedClasses()` buscando referencias (CHECKCAST/INSTANCEOF a interfaces `@Implements`, GETFIELD/PUTFIELD a campos `@Unique`) originadas FUERA del grupo de redefine del namespace -- blocker `external_shape_consumer_unverified`; deteccion de `IMixinConfigPlugin` (targets dinamicos, imposible de garantizar invariantes) -- blocker `mixin_config_plugin_present`. #31: `Tier3DemixApply.TARGETS`/`isEligible`/`resetEligibleTargets`/`replayEligibleTargets`/`ELIGIBLE_REPLAY_TARGETS` eliminados por completo; `Tier3MixinAudit.classifyDemixTargets(namespace,targets,inst)` (publico, fail-closed -- try/catch de metodo COMPLETO, cualquier excepcion bloquea TODOS los targets pedidos) es ahora el clasificador unico RESET/REPLAY/BLOCKED, reusado por `disable()`/`disableGroup`/`Ledger.disableOne`; `disable()`/`computeDisableBytes` ganaron parametro `namespace`. #32: `disableGroup` gana parametro `expectedModes` (nullable) -- si el caller trae el modo que vio en un gate previo, se reclasifica en vivo en el momento de aplicar y se compara EXACTO antes de resolver una sola `Class<?>` o tocar bytecode; diverge -> code `TARGET_MODE_STALE`, nada mutado. `enableGroup` NO recibio este tratamiento (no hay decision de modo en el enable, solo restaura bytes conocidos). #33: `Ledger.disableOne` (tier>=3, `canLowerDecision=true`) ejecuta de verdad: `Tier3MixinAudit.scanTargets(ns)` (nuevo, resuelve el set COMPLETO de targets SIN el `CAP=64` de `plan()`/`targetClasses`, que es solo para diagnostico -- necesario para generalizar de verdad a namespaces con muchos targets) -> `classifyDemixTargets` -> `disableGroup(inst, targets, ns, expectedModes)` -> `modThinReceiptToReply` (confirmado 100% compatible sin cambios, ya que todo camino de fallo de `Tier3DemixApply` sobrescribe `error` con un String). `Ledger.enableOne` gana branch tier3 al inicio: si `Tier3DemixApply.disabledRecordsForNamespace(ns)` tiene entradas, extrae los targets de esos records (NO re-escaneados de disco, que pudo cambiar) y llama `enableGroup`; si no, cae al camino tier1/2 existente sin cambios. #34: el diseno original del plan preveia verificacion pre/post-mutacion via `Instrumentation.retransformClasses` -- pero `Tier3RetransformCaptureSmoke` (corte H, ya cerrado con anterioridad) ya habia probado con evidencia real que ese mecanismo NO entrega un snapshot exacto del bytecode ya redefinido por este agente (`capturedMatchesMixed=false` Y `capturedMatchesBase=false`); usarlo de todos modos habria sido fabricar una verificacion que no es tal -- prohibido de forma no-negociable en este proyecto. Sustituto honesto implementado, que SI funciona con evidencia real: comparar la forma REFLEXIVA de la clase ya cargada ahora mismo (`cls.getDeclaredMethods()/getDeclaredFields()`, 100% fiable, a diferencia de retransform) contra `expectedDeclaredCounts` de los bytes de referencia correctos -- `Tier3LiveCapture.get(target)` (bytes capturados pasivamente en la primera definicion real, corte I) para `disableGroup`, `installedDisabledBytesSnapshot` (lo que se registro haber instalado al desactivar) para `enableGroup` -- ANTES de llamar `redefineClasses`. Si diverge: code nuevo `LIVE_STATE_DRIFT_DETECTED`, aborta sin mutar nada. Ademas, la revision de codigo para esta tarea encontro y corrigio un gap real preexistente: `disableGroup` (a diferencia de `disable()` singular y de `enableGroup`, que ya lo hacian) nunca null-checkeaba `Tier3LiveCapture.get(target)` -- si hubiera sido null y un rollback se disparaba mas tarde, `new ClassDefinition(cls, null)` habria lanzado NPE DESPUES de que el redefine inicial YA hubiera mutado el target, exactamente la corrupcion de estado vivo no detectada que este corte exige prevenir; corregido con el mismo patron `LIVE_BYTES_MISSING` (fail antes de mutar) ya usado en el resto del archivo.
Files touched: `agent/src/dev/mksa/agent/Tier3DemixApply.java` (TARGETS/isEligible retirados, DemixMode/TargetClassification consumidos, expectedModes/anti-TOCTOU, DISABLED_BY_NS, schema+rollback en disableGroup/enableGroup, verificacion pre-mutacion LIVE_STATE_DRIFT_DETECTED, null-check LIVE_BYTES_MISSING agregado en disableGroup), `agent/src/dev/mksa/agent/Tier3MixinAudit.java` (DemixMode/TargetClassification/classifyDemixTargets, escaner de consumidores externos de forma, deteccion IMixinConfigPlugin, scanTargets nuevo, mode por target en buildDemixPlan), `agent/src/dev/mksa/agent/Agent.java` (t3.demixApplyDisable/t3.demixGroupDisable actualizados a las nuevas firmas), `agent/src/dev/mksa/agent/Ledger.java` (disableOne ejecuta de verdad, enableOne gana branch tier3, comentario stale en modThinCascadeTargets corregido), `docs/log.txt`, `docs/session-handoff.md`.
Command/check expected: `bash agent/build.sh` (javac --release 8 de los tres jars), `cargo check --bin verify-fase1`. Pendiente para cerrar de verdad: `cargo run --bin verify-fase1 -- --tier1-7` contra una Instancia real con el agente adjunto; regresion manual disable/enable repetida sobre `fabric-block-view-api-v2`/`chat_heads` comparando receipts; inyeccion de fallo forzando un target con bytes esperados incorrectos confirmando que el rollback automatico se dispara; ciclo completo disable->enable sobre un TERCER mod Tier 3 real nunca antes en `TARGETS` (candidato ya identificado en cortes previos: BiomesOPlenty, `biomesoplenty/init/ModCreativeTab.class`, Tier 3, con mixins propios via `biomesoplenty.fabric.mixins.json` -- nunca auditado, seria la primera prueba real de generalizacion mas alla de los 2 namespaces historicos).
Expected success signal: `t3.demixGroupDisable`/via `modThinDisable` en un namespace Tier 3 nunca antes en `TARGETS` (ej. BiomesOPlenty) queda ejecutable end-to-end si `classifyDemixTargets` lo aprueba (sin escaner de co-owners bloqueando, sin consumidor externo de forma detectado, sin `IMixinConfigPlugin`); alternar disable/enable repetidamente sobre `chat_heads`/`fabric-block-view-api-v2` sin regresion visible respecto al comportamiento pre-corte; forzar un mismatch de schema dispara `ROLLED_BACK` visible en el receipt, nunca exito silencioso; `class_303` sigue excluido del grupo de redefine de `chat_heads` y el escaner nuevo lo mantiene bloqueado por evidencia real (no memoria historica).
Verification:
- OK `bash agent/build.sh` (javac --release 8 de los tres jars, limpio en cada iteracion de las 8 tareas, solo warnings Java 8 obsoleto/unchecked conocidos).
- OK `cargo check --bin verify-fase1` (solo warnings preexistentes de `id` no leido, ninguno nuevo).
- OK revision de codigo linea por linea de cada camino nuevo (anti-TOCTOU, rollback automatico, verificacion pre-mutacion, wiring de Ledger) contra el diseno del plan aprobado, incluyendo el gap real de null-check encontrado y corregido en #34.
- PENDIENTE runtime real: sin una JVM de Minecraft con el agente adjunto disponible en este entorno, NO se corrio ningun toggle real disable/enable in-game durante este segmento -- ni la regresion sobre los 2 namespaces historicos, ni la inyeccion de fallo, ni la generalizacion a un tercer mod, ni `--tier1-7`. Se declara explicitamente en vez de afirmar "verificado" sin haberlo corrido.
Recovery notes: Si se retoma este corte, el siguiente paso es puramente de EJECUCION (no de diseno): adjuntar el agente a una Instancia real con `chat_heads`/`fabric-block-view-api-v2` instalados y correr el ciclo disable/enable varias veces comparando receipts contra el comportamiento pre-corte; despues, para probar generalizacion real, usar BiomesOPlenty (candidato ya identificado, nunca auditado) como el "tercer mod" que exige el plan. Si `classifyDemixTargets` bloquea BoP por `external_shape_consumer_unverified` o `mixin_config_plugin_present`, eso es exactamente el comportamiento fail-closed esperado -- no un bug a corregir sin evidencia adicional. Para inyeccion de fallo controlada (probar `ROLLED_BACK`), el candidato mas simple es forzar `computeDisableBytes` a devolver bytes con un metodo/campo de mas o de menos respecto al schema esperado, sobre un target sintetico como `Tier3ControlledDemixTarget` (ya usado por otros smokes de este archivo) en vez de arriesgar un mod real.

Fixes adicionales (mismo corte, encontrados por revision de codigo propia mientras se investigaba como el wiring de #33 llega de verdad al jugador, ningun mensaje nuevo del usuario): `Ledger.modThinListMods()` calculaba `supportedDisable` como `tier>=1 && tier<=2` -- un tope duro que dejaba el boton de disable/enable SIEMPRE gris para cualquier mod Tier 3 en el panel in-game, sin importar que `disableOne`/`enableOne` (#33) ya ejecutaran de verdad para tier>=3. Esto violaba directamente el mandato "conectar las cosas end to end" del usuario: el backend estaba conectado pero el punto de entrada real (el click del jugador) nunca podia alcanzarlo. Corregido a `tier>=1` (sin tope superior) -- la seguridad real sigue viviendo enteramente en el gate (`canLowerDecision`/`classifyDemixTargets`), un click sobre un mod Tier 3 no viable simplemente falla limpio con el mismo banner de error de cualquier otro fallo de `runAction`, nunca finge soporte. De forma relacionada, `ModsScreen.java` tenia un tooltip stale ("Solo Tier 1 in-process") en el boton gris, ya desactualizado antes de este fix (Tier 2 ya estaba soportado hace tiempo) y doblemente stale despues -- corregido a texto generico "Tier no soportado" (unico caso restante: Tier 0, nada que desactivar). Ambos fixes build-verificados en los tres surfaces (`agent/build.sh`, `mod-thin/build.sh`, `cargo check --bin verify-fase1`).

Task #36 (verificacion runtime real, con JVM de Minecraft adjunta disponible en este entorno): se ejecuto por primera vez lo que quedaba PENDIENTE arriba. Bug real encontrado: `classifyDemixTargets` (Tier3MixinAudit.java) y su logica paralela en `buildDemixPlan` clasificaban `RESET` incondicional en la rama "sin co-owner", sin mirar `fieldSafety.allKnown`/`externalShapeConsumers.allKnown`/`configPluginPresent` (senales ya computadas mas arriba en el mismo metodo, simplemente descartadas en esa rama) -- exactamente el patron de peligro de corte AN: `class_338$1` es una clase NO-mixin externa, nunca puede aparecer como co-owner, asi que "sin co-owner" nunca implico "seguro". Confirmado en vivo: `class_303`/`class_303$class_7590` (chat_heads) tenian las 3 senales en rojo simultaneo y aun asi clasificaban `mode:"RESET"`. Nunca hubo corrupcion real porque el gate a nivel namespace (`safeToExecute`) seguia bloqueado igual -- el bug estaba en un dato de diagnostico/plan, no en una ejecucion real. Usuario aprobo el fix explicitamente via `AskUserQuestion` antes de aplicarse. Fix: mover las 3 comprobaciones antes del branch de co-owner en ambos lugares (`classifyDemixTargets` y el loop de `buildDemixPlan`). Verificado en vivo tras el fix: `class_303`/`class_303$class_7590` clasifican `mode:"BLOCKED"`. De paso se encontro y corrigio un bug propio del harness (`verify-fase1.rs`): el check `class303_present` testeaba presencia en `demixPlan.targetPlans`, pero ese array es diagnostico de TODOS los targets conocidos con su `mode` -- no "el grupo que se redefine" -- por lo que nunca podia fallar de forma significativa; corregido a assertar `mode=="BLOCKED"` para ambas clases especificamente (evitando asi reportar "sigue fallando" cuando en realidad el codigo ya estaba arreglado y solo el test estaba mal, descubierto por inspeccion manual de JSON crudo antes de reportar cualquier conclusion). `cargo run --bin verify-fase1 -- --tier3-e2e-regen` paso completo, exit code 0 (run `b9u7uim9t`): `class_303`/`class_303$class_7590` BLOCKED, generalizacion a `biomesoplenty` (namespace nunca en la vieja `TARGETS`) confirmada, cierre limpio del juego, region files a salvo.

`--tier1-7` se intento aparte para descartar regresion en Tier 1/2 -- fallo en el sub-test "Corte tabs" (mcwstairs `tabVeto`, `paramsCaptured=0` en vez de `>=1`), que segun sus propias instrucciones impresas exige que un humano abra fisicamente el inventario creativo y observe 15s en vivo durante la corrida -- estructuralmente imposible de satisfacer en una corrida desatendida en background. El diff de este segmento esta confirmado aislado a `Tier3MixinAudit.java`/`verify-fase1.rs` (ningun archivo Tier 1/2 tocado), asi que esta falla NO se considera una regresion introducida por este corte -- queda como pregunta abierta para el usuario: aceptar el alcance de regresion ya probado (diff aislado) o correr `--tier1-7` el mismo interactivamente para una senal definitiva.

Limite estructural aceptado explicitamente por el usuario (via `AskUserQuestion`, decision: "Dejarlo como limite estructural declarado"; NO a resolver con una verificacion mas debil): `canLowerDecision=true` sigue practicamente inalcanzable sobre datos reales de gameplay porque `buildExternalShapeConsumers` es fail-closed ante clases lambda sintetizadas por la JVM sin recurso de classpath legible (`cl.getResource(...)` devuelve `null`) -- en una JVM real con ~24000+ clases cargadas esto produce `allKnown=false` casi universal ("ceguera a lambdas"). Esto deja el path de fault-injection/rollback (`ROLLED_BACK`) sin ejercitar en vivo tambien, porque el gate nunca deja pasar ningun namespace real a `disableGroup`. Declarado honesto en vez de debilitar la verificacion para forzar un `true` artificial.

Files touched (task #36, adicional a los ya listados arriba): `agent/src/dev/mksa/agent/Tier3MixinAudit.java` (fix en `classifyDemixTargets` y `buildDemixPlan`), `launcher/src-tauri/src/bin/verify-fase1.rs` (fix del check `class303_present` -> assert por `mode`), `docs/log.txt`, `docs/session-handoff.md`.
Verification (task #36): OK `bash agent/build.sh` (limpio); OK `cargo build --bin verify-fase1` (limpio, solo warnings preexistentes de `id`); OK `cargo run --bin verify-fase1 -- --tier3-e2e-regen` exit 0 (run `b9u7uim9t`, class_303/class_303$class_7590 BLOCKED, generalizacion biomesoplenty confirmada).

Cierre final del corte: el usuario corrio `cargo run --bin verify-fase1 -- --tier1-7` interactivamente (confirmando a simple vista el sub-test manual "Corte tabs"/mcwstairs -- boton STAIRSITEMGROUP desaparece/reaparece, mismo modelo de confianza que corte 6) y confirmo explicitamente: **VERIFICACION OK completo, exit code 0, sin ningun `✘` en el resto de la suite**. Con esto, el ultimo item pendiente del plan queda resuelto sin necesidad de debilitar ni fabricar ninguna verificacion -- regresion Tier 1/2 confirmada por corrida real, no por inferencia de diff aislado. Las 9 tareas del plan (#28-#36) quedan CERRADAS. Corte tender-seeking-fern completo.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-10
Objective: Parte 2 (clasificacion automatica de seguridad de campos @Unique): reemplazar la investigacion manual mod-por-mod (el registro `PRESERVE_FIELDS` con una sola entrada tipeada a mano, corte AM) por un escaneo de bytecode que clasifica automaticamente cada campo `@Unique` agregado por un mixin en tres cajas -- seguro, necesita preservarse (auto-registrado), o desconocido/bloquear por defecto -- cubriendo todos los mods, incluidos los futuros, sin trabajo manual por namespace.
Intent: Extender el parser binario hecho a mano `ClassFile` (`Tier3MixinAudit`, sin ASM, paquete `dev.mksa.agent`) para leer DENTRO de los cuerpos de metodo (atributo `Code`, saltado hasta ahora) y resolver referencias a campos (`GETFIELD`/`PUTFIELD`/`GETSTATIC`/`PUTSTATIC`) via un escaner de instrucciones fail-closed (`scanCode`, tabla `FIXED_OPERAND_BYTES` explicita, throw ante opcode desconocido). Clasificador `buildFieldSafety`: para cada campo `@Unique` candidato, escanea vanilla base (`BASE_BYTES`) + bytecode de mixins co-owner buscando referencias sobrevivientes; `preserve` si hay referencia real (o es un `@Accessor`/`@Invoker` co-owner, caso especial pre-weave), `unknown` (fail-closed) ante reflexion/`MethodHandles`/`Unsafe`/lambdas reales/bytecode base ausente/fallo de parseo, `safe` en cualquier otro caso. Wireado a `buildDemixPlan`/`buildTxGate` con el mismo patron ya usado por `runtimeOrder`/`transformerQuiescence` (cortes K/L): `fieldSafety` por target, `allTargetsFieldSafetyVerified`, `fieldSafetyTrust`, blocker `unique_field_safety_unverified` entrando al AND de `canLowerDecision`. `Tier3DemixApply.PRESERVE_FIELDS` (registro estatico hardcodeado) eliminado; `snapshotPreservedFields`/`restorePreservedFields` llaman en vivo a `Tier3MixinAudit.preserveFieldsForTarget(namespace, target)`.
Updated: 2026-07-10
Current intent: N/A, corte cerrado en la misma sesion tras 3 bugs reales encontrados y corregidos durante la propia verificacion.
Completed: 2026-07-10
Result: Parte 2 cerrada, las 7 verificaciones del plan pasaron. Durante la verificacion (no antes) se encontraron y corrigieron 3 bugs reales en la logica nueva, sobre el caso real `rendererPools`/`class_11228` (chat_heads): (A) los candidatos de campo `@Unique` solo se buscaban entre mixins del namespace victima, pero `disableGroup` en modo RESET borra TODOS los mixins co-owner del target, no solo los de la victima -- `rendererPools` lo agrega `fabric-rendering-v1` (co-owner), no `chat_heads`; corregido: candidatos = todos los contribuyentes del target, no solo la victima. (B) `invokedynamic` marcaba `risk=true` incondicional para CUALQUIER ocurrencia, pero desde Java 9 la concatenacion comun de strings (`"a"+b`) tambien compila a `invokedynamic` via `StringConcatFactory` -- volvia `preserve` casi inalcanzable en clases reales; corregido resolviendo el bootstrap real (`BootstrapMethods` + `MethodHandle`, tags de constant pool antes descartados en `readCp`) y excluyendo `StringConcatFactory` del riesgo, todo lo demas (`LambdaMetafactory`, bootstrap no resoluble) sigue fail-closed. (C) el escaneo del `.class` crudo de un mixin co-owner nunca encontraba su propio campo `@Unique` porque javac compila `this.campo` dentro de la clase que lo declara con `owner=esa misma clase`, no el target -- Mixin recien re-apunta esa referencia al target al tejer, y el escaneo pre-weave solo buscaba `owner==target`; corregido contando tambien `owner==declaringClass` (capturado via `Header.name`, antes descartado). Los 3 fixes fueron confirmados explicitamente por el usuario antes de aplicarse (uno por uno, no en bloque). Resultado final verificado en runtime real: `rendererPools`/`hasFabricInitialized`/`orderedRenderCommandQueue` clasifican `preserve`/`direct_bytecode_reference`, `allKnown=true`, `unknownTotal=0` (reproduce por escaneo automatico exactamente lo que `PRESERVE_FIELDS` tenia a mano, sin registro hardcodeado); `balm$unpackedLootTable` clasifica `safe`/`no_surviving_reference_found` (control negativo, no sobre-preserva); `balmData`/`forgeBalmData`/`neoForgeBalmData`/`forcedPose` clasifican `unknown`/`reflection_lambda_or_missing_base` (base bytecode ausente, control de riesgo fail-closed) y bloquean correctamente `canLowerDecision` con `unique_field_safety_unverified`.
Files touched: `agent/src/dev/mksa/agent/Tier3MixinAudit.java` (extension de `ClassFile`: captura de `Code`, `scanCode`, `FIXED_OPERAND_BYTES`, `refAt`, `readClassBootstrapMethods`, `methodHandleOwner`, `bootstrapOwnerFor`, `isReflectionRisk`, `buildFieldSafety`, `accessorFieldName`, `preserveFieldsForTarget`; `readCp` ya no descarta Fieldref/Methodref/InterfaceMethodref/NameAndType/MethodHandle/Dynamic/InvokeDynamic), `agent/src/dev/mksa/agent/Tier3DemixApply.java` (`PRESERVE_FIELDS` eliminado, `snapshotPreservedFields`/`restorePreservedFields` con parametro `namespace`, lookup en vivo), `agent/src/dev/mksa/agent/Tier3FieldScanSmoke.java` (nuevo, smoke dedicado `invokeinterface`/`wide`), `launcher/src-tauri/src/bin/verify-fase1.rs` (`tier3_parte2_fieldsafety_regression_check`, flag `--tier3-parte2-fieldsafety-regression`), `docs/log.txt`, `docs/session-handoff.md`.
Command/check expected: `bash agent/build.sh` (javac --release 8 de los tres jars, sin ASM nuevo en `dev.mksa.agent`), `cargo run --bin verify-fase1 -- --tier3-parte2-fieldsafety-regression` (nuevo, regresion dedicada), `cargo run --bin verify-fase1 -- --tier3-audit` (suite general, sin regresion).
Expected success signal: `t3.fieldScanSmoke` completa sin `IOException` de opcode desconocido sobre clases JDK reales (invokeinterface real garantizado) mas mixins reales si hay mods instalados; `preserveFieldsForTarget("chat_heads","net.minecraft.class_11228")` devuelve `{"net.minecraft.class_11228":["rendererPools", ...]}` sin que `PRESERVE_FIELDS` exista mas en el codigo; control negativo (`balm$unpackedLootTable`) clasifica `safe`; control de riesgo (base bytecode ausente) clasifica `unknown` y `canLowerDecision=false` con el blocker `unique_field_safety_unverified` presente; `--tier3-audit` sin regresion.
Verification:
- OK `bash agent/build.sh` (javac --release 8 de los tres jars, limpio en cada iteracion de los 3 fixes, solo warnings Java 8 obsoleto/unchecked conocidos).
- OK `t3.fieldScanSmoke`: clases JDK (`ArrayList`/`HashMap`/`Collections`/`AbstractCollection`, invokeinterface real garantizado sin depender de mods instalados) + mixins reales cuando hay mods -- todas parsean limpio, sin desincronizacion de `invokeinterface`(185)/`wide`(196).
- OK `cargo run --bin verify-fase1 -- --tier3-parte2-fieldsafety-regression` (runtime real, tras los 3 fixes): `rendererPools`/`hasFabricInitialized`/`orderedRenderCommandQueue` clasifican `preserve`/`direct_bytecode_reference`, `allKnown=true`, `unknownTotal=0`; reproduce por escaneo automatico exactamente el registro que `PRESERVE_FIELDS` tenia hardcodeado.
- OK control negativo (mismo run): `balm$unpackedLootTable` clasifica `safe`/`no_surviving_reference_found`.
- OK control de riesgo (mismo run): `balmData`/`forgeBalmData`/`neoForgeBalmData`/`forcedPose` clasifican `unknown`/`reflection_lambda_or_missing_base`, `allTargetsFieldSafetyVerified=false`, `canLowerDecision=false`, blocker `unique_field_safety_unverified` presente.
- OK `cargo run --bin verify-fase1 -- --tier3-audit` (regresion general completa tras los 3 fixes): 105 aserciones `✔`, 0 `✘`, exit code 0, `VERIFICACION OK`.
Recovery notes: los 3 bugs (A/B/C) son interdependientes y acumulativos -- ninguno por separado alcanza para clasificar `rendererPools` como `preserve` (A sin B/C deja el campo como candidato pero igual `unknown`; B sin C sigue sin encontrar la auto-referencia del mixin a su propio campo). Si se toca de nuevo `scanCode`/`buildFieldSafety`, recordar: (1) `owner==declaringClass` (ademas de `owner==target`) es necesario porque el `.class` de un mixin co-owner es SIEMPRE pre-weave -- Mixin nunca re-escribe el `.class` en disco, solo el bytecode vivo tejido; (2) `invokedynamic` requiere resolver el bootstrap real, no asumir riesgo por la sola presencia del opcode (rompe con cualquier concatenacion de string moderna); (3) el registro `Tier3DemixApply.PRESERVE_FIELDS` ya no existe -- cualquier caso nuevo de campo a preservar debe salir del escaneo automatico, no de un `Map` a mano. Pendiente honesto fuera de alcance (documentado en el plan, no bloqueante para este corte): `Ledger.disableOne` (tier>=3) sigue devolviendo `TIER3_TX_APPLY_NOT_IMPLEMENTED` sin llegar a `Tier3DemixApply` -- el gate real de produccion para el toggle in-game de Tier 3 sigue como trabajo futuro ya identificado; esta Parte 2 garantiza que, el dia que ese wiring se complete, el mecanismo de preservacion ya este auto-poblado.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-09
Objective: Corte tabs: toggle de pestana de inventario creativo en sincronia con disable/enable, universal a los 3 tiers -- mecanismo generico (no especifico a ningun mod), consistente con la politica de "categoria, no caso por caso" del proyecto. Arranco por `mcwstairs` (Tier 1), paso por dos bugs reales encontrados en corridas del usuario, y cerro con el cableado explicito a Tier 3 por instruccion directa del usuario ("esto debe aplicar a todos los tiers, esto es muy universal... El mod agrega pestañas al creativo? pues haz lo mismo que hiciste con stairs").
Intent: Investigacion de bytecode real (jar intermediary + mappings Yarn) confirmo el punto canonico unico por el que pasa CUALQUIER item de CUALQUIER mod al entrar a CUALQUIER pestana: `class_1761$class_7703.method_45417` (`CreativeModeTab$ItemDisplayBuilder.accept(ItemStack, TabVisibility)`, unico metodo real ademas del constructor, confirmado por `javap`). Vainilla ya oculta sola el boton de una pestana `CATEGORY` vacia (`class_1761.method_47311`/`shouldDisplay`->`hasStacks`) -- vetar la poblacion de items basta, sin tocar `Registries.ITEM_GROUP` ni la GUI.
Updated: 2026-07-09
Current intent: N/A, corte cerrado en la misma sesion tras dos bugs encontrados+corregidos y la extension a Tier 3.
Completed: 2026-07-09
Result: Tres piezas cerradas en este corte.
(1) Bug registry id (encontrado en la corrida real del usuario contra `mcwstairs`): `rebuildAllCreativeTabs` original iteraba `Wcg.liveRegistry(..., "minecraft:item_group")`, id de registro equivocado -- corregido a `"minecraft:creative_mode_tab"`. VERIFICADO por el usuario: el boton de `mcwstairs` desaparece por completo (no vacio) y reaparece con items reales.
(2) Bug de reflow (encontrado via `bugcreativo.png`, screenshot del usuario): al ocultar una pestana quedaba un hueco vacio en la fila en vez de que las pestanas siguientes se corrieran a ocupar el lugar (comportamiento nativo esperado cuando un mod deja de estar cargado). Causa raiz confirmada por `javap` sobre `client-intermediary.jar` + `fabric-item-group-api-v1`: `CreativeModeTab.row/column` son `private final`, asignados una sola vez en construccion; `CreativeInventoryScreen` posiciona cada boton directo sobre esos campos, nunca por indice en la lista filtrada visible -- ocultar el contenido nunca recalcula la posicion de las demas. El repaginado real (paginacion Fabric, `TABS_PER_PAGE=10`) vive en `CreativeModeTabsMixin.paginateGroups`, inyectado en el HEAD de `ItemGroups.updateEntries(ItemDisplayParameters)` (`class_7706.method_47337`). Fix: `Ledger.rebuildAllCreativeTabs()` reescrito para invocar `updateEntries` reflectivamente DOS veces seguidas en vez de iterar `buildContents` por pestana -- la 1a pasada vuelca el veto a `shouldDisplay()`, la 2a deja que `paginateGroups` (que lee `shouldDisplay()` en su propio HEAD) cierre el hueco/repagine con el estado fresco. Nuevos campos `itemGroupsClass`/`updateEntriesMethod`/`updateEntriesFailed` + `resolveUpdateEntries(params)` (desambiguacion estructural: unico metodo `static void` de 1 parametro en la clase, per `feedback_reflective_disambiguation`) reemplazan a los viejos `buildContentsMethod`/`buildContentsFailed`/`resolveBuildContents` (eliminados). Contadores diagnostico (`tabRebuildInvoked`/`tabRebuildInvokeErrors`/etc.) mantienen nombre/getter, solo cambia que cuentan (`invoked=2` esperado en exito). VERIFICADO por el usuario en vivo: "oye pues funciono, todo correcto".
(3) Extension universal a los 3 tiers (instruccion directa del usuario, no solo confirmacion): investigacion de rutas de codigo confirmo que Tier 1 y Tier 2 YA comparten el mismo mecanismo gratis -- `Ledger.modThinDisable`/`modThinEnable` solo bifurcan por tier cuando `tier>=3`; para tier 1/2 llaman sin condicion a `InProcess.tier1Disable`/`tier1Enable`, que son las funciones donde viven `txStepTabVeto`/`txStepTabRestore` (Tier 2 solo agrega `runtimeDisable`/`runtimeRestore` encima, sin tocar el paso de pestana). Tier 3 SI necesitaba cableado nuevo: la unica via Tier 3 que hoy ejecuta un disable/enable real de mod (no solo diagnostico) es `Tier3DemixApply.disableGroup`/`enableGroup` (usada por `t3.demixGroupDisable`/`t3.demixGroupEnable`, namespace-aware via `targetsForNamespace`) -- la via "oficial" (`modThinDisable` con `tier>=3`) sigue sin ejecutar nada real hoy (falla cerrado con `TIER3_DEMIX_NOT_EXECUTABLE`/`TIER3_TX_APPLY_NOT_IMPLEMENTED` para practicamente todo namespace), asi que no hay nada que cablear ahi sin inventar ejecucion inexistente. `disableGroup`/`enableGroup` ganaron un tercer parametro `String namespace` (nullable, compat con callers que no lo tengan) + nuevos helpers privados `txStepTabVeto`/`txStepTabRestore` (copia exacta del patron de `InProcess`, mismo Map de detalle, mismo best-effort -- nunca lanzan, nunca bloquean el redefine). Insertados justo despues de crear el `TxReceipt`, antes del trabajo sustantivo (mismo lugar relativo que en `tier1Disable`/`tier1Enable`). `Agent.java` actualizado para pasar el `namespace` ya disponible en `t3.demixGroupDisable`/`t3.demixGroupEnable` a ambas llamadas.
Caso real identificado para probar esto (diferido, no bloqueante para este corte): `BiomesOPlenty-fabric-1.21.11-21.11.0.32.jar` (Instancia 3) tiene `biomesoplenty/init/ModCreativeTab.class` -- pestana de creativo propia confirmada, y es Tier 3. Pero sus mixins reales (`MixinEntity`, `MixinBloodFluid`, `MixinLiquidNullFluid`, `MixinScreenEffectRenderer`, via `biomesoplenty.fabric.mixins.json`) nunca fueron registrados en `Tier3DemixApply.TARGETS`/`targetsForNamespace` -- ningun target conocido, `t3.demixGroupDisable(namespace="biomesoplenty")` fallaria hoy con `NAMESPACE_NOT_ELIGIBLE` antes de siquiera llegar al step `tabVeto` recien cableado. Auditar BoP (targets, RESET vs REPLAY, co-owners, verificar `canLowerDecision`) es trabajo comparable a los cortes AG-AN de `chat_heads` -- explicitamente diferido por decision del usuario ("Diferir BoP, cerrar el corte tabs ahora") a una tarea aparte.
Files touched: `agent/src/dev/mksa/agent/Ledger.java`, `agent/src/dev/mksa/agent/Tier3DemixApply.java`, `agent/src/dev/mksa/agent/Agent.java`, `docs/log.txt`, `docs/session-handoff.md`. (Bridge.java/LedgerSink.java/LedgerTransformer.java/InProcess.java/verify-fase1.rs tocados en la parte 1/2 de este mismo corte, sin cambios adicionales en la parte 3 -- el cableado a Tier 3 reutilizo la API publica ya expuesta por Ledger sin tocar el hook de bytecode ni el IPC de Tier 1.)
Command/check expected: `bash agent/build.sh` (javac --release 8 de los tres jars), `cargo build --bin verify-fase1`.
Expected success signal: build limpio en ambos lados (logrado); `tx.tier1Disable/tier1Enable.steps` incluyen `tabVeto`/`tabRestore` con `invoked=2`/`invokeErrors=0` (Tier 1/2, verificado real por el usuario); `t3.demixGroupDisable/demixGroupEnable` en un namespace Tier 3 realmente ejecutable incluirian el mismo par de steps con el mismo shape (compilado y cableado, PENDIENTE de runtime real hasta que exista un namespace Tier 3 auditado -- BoP es el candidato, diferido).
Verification:
- OK `bash agent/build.sh` (javac --release 8 de los tres jars, solo warnings Java 8 obsoleto/unchecked conocidos).
- OK `cargo build --bin verify-fase1` (solo warnings preexistentes de `id` no leido, ninguno nuevo).
- OK runtime real Tier 1 (`--tier1-7` contra Instancia 3 con `mcwstairs`): usuario confirmo visualmente boton desaparece por completo, reflow cierra el hueco corriendo las pestanas siguientes, reaparece con items reales al reactivar.
- PENDIENTE runtime real Tier 3: no hay hoy un namespace Tier 3 auditado/ejecutable con pestana de creativo propia en la instancia de prueba (BoP calza pero no esta auditada) -- el cableado quedo verificado por compilacion y por simetria de codigo con el path Tier 1 ya probado, no por observacion visual en vivo todavia.
Recovery notes: El bloqueador real para probar Tier 3 end-to-end no es el veto de pestanas (ya cableado) sino que ningun mod Tier 3 real tiene su registro de targets/mixins auditado salvo `chat_heads` (sin pestana propia) y el grupo `fabric-block-view-api-v2` (API interna sin pestana propia) -- BoP es el primer candidato con pestana real, queda como tarea aparte explicita si el usuario quiere retomarla: auditar `biomesoplenty` (targets, RESET vs REPLAY, co-owners) siguiendo el mismo patron que los cortes AG-AN de `chat_heads`.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar T3 corte L (Pilar 2): auto-quiescencia de transformers MKSA vs MKSA -- alcance acotado por decision explicita del usuario a evidencia estatica de solapamiento entre targets Tier 3 y las clases que el propio `LedgerTransformer` ya muta, excluyendo transformers de terceros y fase de carga de Mixin.
Intent: Por especificacion oficial de `java.lang.instrument.ClassFileTransformer`/`Instrumentation` (javadoc), `Instrumentation.redefineClasses` (primitivo que usara el aplicador transaccional real de Pilar 3) re-dispara TODOS los transformers ya registrados sin importar `canRetransform` -- `LedgerTransformer` (`dev.mksa.agent.ledger`, `canRetransform=true`) muta 9 familias de clases; si un target Tier 3 solapa, un futuro `redefineClasses` re-dispara ese transformer con riesgo real de doble inyeccion. `Tier3MixinAudit` (paquete `dev.mksa.agent`, sin ASM en el classpath, no puede importar `LedgerTransformer` -- misma restriccion de corte K) gana un espejo estatico manual `OWN_TRANSFORMER_TARGET_FAMILIES` (9 familias, sync manual documentado) y `buildTransformerQuiescence(target)`, comparacion pura de strings (`available` SIEMPRE `true`, sin cobertura parcial posible). `buildDemixPlan` acumula `transformerQuiescence` por target (mismo patron que `runtimeOrder`, corte K) y agrega `transformerQuiescenceTrust`. `buildTxGate` reemplaza la derivacion hardcodeada (`!requiresTransformerQuiescence`, constante fija que siempre daba `false`) por comparacion contra el agregado real. `canLowerDecision` debe seguir `false`: Pilares 3-4 fuera de alcance.
Updated: 2026-07-03 13:49
Current intent: N/A, corte cerrado en una sola sesion.
Completed: 2026-07-03 13:49
Result: T3 corte L cerrado, Pilar 2 resuelto en su alcance acotado. `Tier3MixinAudit` suma la tabla estatica `OWN_TRANSFORMER_TARGET_FAMILIES` (nombres dotted, copiados a mano de las 9 constantes `String[]` de `LedgerTransformer`), `buildTransformerQuiescence(target)`, wiring en `buildDemixPlan` (`transformerQuiescence` por target, `transformerQuiescenceTrust`/`transformerQuiescenceModel`/`transformerQuiescenceEvidenceAvailable` agregados), nuevo riesgo `own_transformer_overlap_risk` en `demixRisks`, y la derivacion real en `buildTxGate` (`transformerQuiesced = "no_own_transformer_overlap_detected".equals(transformerQuiescenceTrust)`, mismo patron de auto-derivacion por string que `runtimeOrderTrusted` de corte K). Runtime verificado: `balm` SI solapa con LedgerTransformer -- `transformerQuiescenceTrust=own_transformer_overlap_unresolved`, familias reales `LevelChunk` y `RecipeManager`, `txGate.transformerQuiescenceVerified=false`, blocker `transformer_quiescence_unverified` presente; resultado real observado y asertado, no forzado de antemano. Control negativo intacto: `canLowerDecision=false`, `tx.disable` sigue `TIER3_DEMIX_NOT_EXECUTABLE`.
Files touched: `agent/src/dev/mksa/agent/Tier3MixinAudit.java`, `launcher/src-tauri/src/bin/verify-fase1.rs`, `docs/proyecto.md` (§3.30 nuevos bullets), `docs/log.txt`.
Command/check expected: `javac --release 8` (via `agent/build.sh`), rebuild nativo PowerShell de `agent/dist/fable-agent.jar` (`agent/tmp-build/rebuild.ps1`), `cargo check`/`cargo build --bin verify-fase1`, runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados y salida `tier3-l-smoke.txt`.
Expected success signal: `demixPlan.transformerQuiescenceEvidenceAvailable=true` (siempre, estatica); al menos un `targetPlan.transformerQuiescence` con `available=true` y `model=own_transformer_overlap_static_v1`; `transformerQuiescenceTrust`/`txGate.transformerQuiescenceVerified`/blocker `transformer_quiescence_unverified` reflejando fielmente el solapamiento real observado (sin forzar); `canLowerDecision` sigue `false`.
Verification:
- OK `bash agent/build.sh` (javac --release 8 de los tres jars: bridge, ledger con ASM, agente sin ASM/bridge/ledger sueltos; solo warnings Java 8 obsoleto/unchecked conocidos). Este corte no toco ASM ni `dev.mksa.agent.ledger` -- solo `Tier3MixinAudit.java` (paquete `dev.mksa.agent`).
- OK rebuild nativo PowerShell (`agent/tmp-build/rebuild.ps1`) de `agent/dist/fable-agent.jar` (15197508 bytes), checks internos del script pasaron (sin ASM/bridge/ledger sueltos, `Tier3MixinAudit.class` presente).
- OK `cargo check --bin verify-fase1` y `cargo build --bin verify-fase1` (solo 5 warnings preexistentes de `id` no leido).
- OK runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados, salida `tier3-l-smoke.txt`: candidato `balm`, `transformerQuiescenceEvidenceAvailable=true`, al menos un `targetPlan.transformerQuiescence` con `model=own_transformer_overlap_static_v1`; resultado real: `transformerQuiescenceTrust=own_transformer_overlap_unresolved` (familias solapadas `LevelChunk`, `RecipeManager`), `txGate.transformerQuiescenceVerified=false`, blocker `transformer_quiescence_unverified` presente; las 7 aserciones nuevas de corte L que corrieron pasaron (`✔`), cero `✘`; cortes A-K intactos; `VERIFICACIÓN OK`, cierre WM_CLOSE limpio (region files a salvo).
Recovery notes: `OWN_TRANSFORMER_TARGET_FAMILIES` es una copia manual de las constantes de `LedgerTransformer` -- si ese archivo cambia sus familias de clases mutadas, esta tabla debe sincronizarse a mano (comentario explicito en el codigo). El resultado real (`balm` solapa con `LevelChunk`/`RecipeManager`) confirma que la evidencia estatica detecta casos reales, no solo el caso vacio -- valida el diseño sin necesitar un mod de prueba especial. Pilar 2 (auto-quiescencia MKSA vs MKSA) queda resuelto en su alcance acotado; proxima frontera son los Pilares 3-4 (aplicador transaccional real sobre targets Minecraft, rollback demostrado bajo fallo) -- `canLowerDecision` seguira `false` hasta que ambos esten resueltos. Fuera de alcance confirmado y no revisitado en este corte: quiescencia de terceros, fase temporal de Mixin, `LiveClassModel` (diferido hasta despues de Pilar 4, decision ya en memoria persistente).

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar T3 corte K (Pilar 1b): orden runtime real de aplicacion de mixins via hook ASM sobre `MixinInfo.createContextFor`, segundo y ultimo sub-corte de Pilar 1 (Pilar 1a, atribucion via `@MixinMerged`, ya cerrado en corte J).
Intent: Instrumentar `MixinInfo.createContextFor(TargetClassContext)` (confirmado por `javap -p -c`) con un `ClassFileTransformer` activo (`Tier3MixinOrderCapture`, nuevo) que inyecta `Bridge.onMixinApplyOrder(targetClassName, mixinName, priority)` a la entrada del metodo — invocado una vez por mixin por target, en el orden real con que `MixinApplicatorStandard.apply` itera el set ya ordenado por prioridad. A diferencia de `Tier3LiveCapture` (observacional, vive en `dev.mksa.agent`), este SI inyecta bytecode y por eso debe vivir en `dev.mksa.agent.ledger` (unico paquete con ASM en el classpath, cargado por `Agent.installLedger` en el `URLClassLoader` aislado) y cruzar el bootstrap por el camino Bridge/LedgerSink ya establecido. `Ledger` guarda la secuencia completa (APPEND, no idempotente) por target. `Tier3MixinAudit.buildDemixPlan` suma `runtimeOrder` por target y sube `orderTrust` a `runtime_mixin_order_verified` solo si TODOS los targets del ns verifican exacto contra los contributors declarados — sin forzar cobertura parcial. `canLowerDecision` debe seguir `false`: Pilares 2-4 fuera de alcance.
Updated: 2026-07-03 12:52
Current intent: N/A, corte cerrado en una sola sesion.
Completed: 2026-07-03 12:52
Result: T3 corte K cerrado, Pilar 1 completo (1a+1b). Primer intento coloco `Tier3MixinOrderCapture` en `dev.mksa.agent` (como `Tier3LiveCapture`) y `build.sh` fallo: ese paquete compila deliberadamente SIN ASM en el classpath (para no colisionar con el ASM de Fabric en el classloader del sistema). Corregido moviendo la clase a `dev.mksa.agent.ledger` (junto a `LedgerTransformer`, unico lugar con ASM) y cargandola por reflexion desde `Agent.installLedger` en el mismo `URLClassLoader` aislado, justo despues de `LedgerTransformer.install`. `LedgerSink.onMixinApplyOrder`/`Bridge.onMixinApplyOrder`/`Ledger.onMixinApplyOrder` (nuevo storage `ConcurrentHashMap<String,CopyOnWriteArrayList<MixinOrderEntry>>`) completan el wiring cruzando el bootstrap classloader. `Tier3MixinAudit` suma `buildRuntimeOrder`, submapa `runtimeOrder` por target, agregados `runtimeOrderEvidenceAvailable`/`orderTrust` condicional, y el riesgo `runtime_order_diverges_from_declared`. Runtime verificado: `balm` con `runtimeOrderEvidenceAvailable=true` y al menos un target con secuencia real (`mixin`+`priority`); cobertura parcial honesta (no todos los 41 targets verifican), asi que `orderTrust` se mantiene declarativo y `runtime_mixin_order_unverified` sigue presente — asertado segun el resultado real, no forzado. Control negativo intacto: `canLowerDecision=false`, `tx.disable` sigue `TIER3_DEMIX_NOT_EXECUTABLE`.
Files touched: `agent/src/dev/mksa/agent/ledger/Tier3MixinOrderCapture.java` (nuevo), `agent/src/dev/mksa/bridge/LedgerSink.java`, `agent/src/dev/mksa/bridge/Bridge.java`, `agent/src/dev/mksa/agent/Ledger.java`, `agent/src/dev/mksa/agent/Agent.java`, `agent/src/dev/mksa/agent/Tier3MixinAudit.java`, `launcher/src-tauri/src/bin/verify-fase1.rs`, `docs/proyecto.md` (§3.30 nuevos bullets), `docs/log.txt`.
Command/check expected: `javac --release 8` (via `agent/build.sh`, que compila bridge/ledger/agent como tres jars separados), `cargo check`/`cargo build --bin verify-fase1`, runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados y salida `tier3-k-smoke.txt`.
Expected success signal: `demixPlan.runtimeOrderEvidenceAvailable=true`; al menos un `targetPlan.runtimeOrder` con `available=true`, `model=mixin_info_create_context_entry_hook_v1` y `sequence` con `mixin`/`priority` reales; `orderTrust`/`runtimeMixinOrderTrusted`/blocker `runtime_mixin_order_unverified` reflejando fielmente si la cobertura fue completa o parcial (sin forzar); `canLowerDecision` sigue `false`.
Verification:
- OK `bash agent/build.sh` (javac --release 8 de los tres jars: bridge, ledger con ASM, agente sin ASM/bridge/ledger sueltos; solo warnings Java 8 obsoleto/unchecked conocidos).
- OK `cargo check --bin verify-fase1` y `cargo build --bin verify-fase1` (solo 5 warnings preexistentes de `id` no leido).
- OK runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados, salida `tier3-k-smoke.txt`: candidato `balm`, `runtimeOrderEvidenceAvailable=true`, al menos un `targetPlan.runtimeOrder` con secuencia real de `mixin`+`priority`; cobertura parcial honesta -> `orderTrust` declarativo, `runtimeMixinOrderTrusted=false`, blocker `runtime_mixin_order_unverified` presente; las 7 aserciones nuevas de corte K pasaron (`✔`), cero `✘`; cortes A-J intactos; `VERIFICACION OK`, cierre WM_CLOSE limpio (region files a salvo).
Recovery notes: ASM SOLO puede vivir en `dev.mksa.agent.ledger` (cargado por `Agent.installLedger` en un `URLClassLoader` aislado child-first para `org.objectweb.asm.*`/`dev.mksa.agent.ledger.*`) — nunca en `dev.mksa.agent` (compila sin ASM adrede, ver comentario de cabecera de `agent/build.sh`). Cualquier transformer ASM-nuevo debe seguir el patron de `LedgerTransformer`: clase/metodo `install` PUBLICOS (se cargan por reflexion, `getMethod` exige acceso publico), cargado desde `Agent.installLedger` justo despues de `LedgerTransformer.install`, nunca invocado directamente desde `Agent.premain`. Pilar 1 (atribucion + orden) queda completo; proxima frontera son los Pilares 2-4 (quiescencia de transformers, aplicador transaccional real sobre targets Minecraft, rollback demostrado bajo fallo) — `canLowerDecision` seguira `false` hasta que los cuatro esten resueltos.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar T3 corte J (Pilar 1a): atribucion runtime real de mixins via la anotacion RUNTIME propia de Mixin `@MixinMerged`, primer sub-corte de dos hacia Pilar 1 (orden runtime real de aplicacion de mixins).
Intent: Parsear `org.spongepowered.asm.mixin.transformer.meta.MixinMerged` (escrita por `MixinTargetContext.methodMerged`/`fieldMerged` sobre cada miembro que Mixin mergea en un target) desde el bytecode vivo ya capturado por `Tier3LiveCapture` (corte I) — sin mecanismo de captura nuevo, solo parseo adicional. Da evidencia real de "que mixin mergeo que miembro y con que prioridad", pero explicitamente NO el orden secuencial completo (eso requiere Pilar 1b: hook ASM sobre `MixinApplicatorStandard.apply`, fuera de alcance). `demixPlan.orderTrust`, `txGate.runtimeMixinOrderTrusted` y `canLowerDecision` deben quedar sin cambios a proposito.
Updated: 2026-07-03 10:40
Current intent: N/A, corte cerrado en una sola sesion.
Completed: 2026-07-03 10:40
Result: T3 corte J cerrado. `Tier3MixinAudit` gana `ClassFile.mixinMergedMembers()`/`parseRuntimeAttribution(byte[])` (reusa integramente `readMembers`/`readAnnotations`/`constantString`, cero cambios al lector de bajo nivel). `buildAttributionDiffs` suma `runtimeMergedMembersTotal`/`runtimeAttributionConfirmed`/`runtimeAttributionSamples`/`runtimeAttributionModel` por diff (cacheado por target); `buildDemixPlan` suma submapa `runtimeAttribution` por target (`distinctRuntimeMixins`/`matchesDeclaredContributors`/`unexpectedRuntimeMixins`) y `demixRisks` gana el riesgo informativo `runtime_attribution_diverges_from_declared`; `buildTxGate` suma el campo informativo `runtimeAttributionEvidenceAvailable` (no alimenta `canLowerDecision`). Runtime verificado: los 41 diffs de `balm` confirman `runtimeAttributionConfirmed=true` con muestras reales de `member`/`priority`. Control negativo intacto: `orderTrust` sigue declarativo, `runtimeMixinOrderTrusted=false`, blocker `runtime_mixin_order_unverified` presente, `canLowerDecision=false`, `tx.disable` sigue `TIER3_DEMIX_NOT_EXECUTABLE`.
Files touched: `agent/src/dev/mksa/agent/Tier3MixinAudit.java`, `launcher/src-tauri/src/bin/verify-fase1.rs`, `docs/proyecto.md` (§3.30 nuevos bullets), `docs/log.txt`.
Command/check expected: `javac --release 8` agent (bridge compilado aparte, agente con `-cp out-bridge` excluyendo bridge/ledger), rebuild nativo PowerShell de `agent/dist/fable-agent.jar`, `cargo check`/`cargo build --bin verify-fase1`, runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados y salida `tier3-j-smoke.txt`.
Expected success signal: `runtimeAttributionModel=mixin_merged_runtime_evidence_v1` presente top-level; al menos un diff con `runtimeAttributionConfirmed=true` y muestra real; `demixPlan.targetPlans` incluye `runtimeAttribution` real; `txGate.runtimeAttributionEvidenceAvailable` refleja el agregado; control negativo: `orderTrust`/`runtimeMixinOrderTrusted`/`canLowerDecision` sin cambios, `tx.disable` sigue bloqueado.
Verification:
- OK `javac --release 8` (bridge -> `/tmp/out-bridge`, luego agente excluyendo bridge/ledger con `-cp /tmp/out-bridge`; solo warnings Java 8 obsoleto/unchecked conocidos).
- OK rebuild nativo PowerShell de `agent/dist/fable-agent.jar` (15191533 bytes, `Tier3MixinAudit.class` incluido, bridge/ledger/sqlite anidados, sin ASM/bridge/ledger sueltos).
- OK `cargo check`/`cargo build --bin verify-fase1`.
- OK runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados, salida `tier3-j-smoke.txt`: candidato `balm`, 41/41 diffs `runtimeAttributionConfirmed=true`, muestras `member`/`priority` reales, `demixPlan.targetPlans` con `runtimeAttribution` real, `txGate.runtimeAttributionEvidenceAvailable=true`; las 13 aserciones nuevas de corte J pasaron (`✔`), `grep -c "✘"` devolvio `0`; cortes A-I intactos; cierre WM_CLOSE limpio (region files a salvo), `VERIFICACION OK`, exit code 0.
Recovery notes: `@MixinMerged` solo cubre miembros mergeados (`@Overwrite`, campos/metodos nuevos, handlers de injector copiados) — NO cubre call-sites `@Inject`/`@Redirect` tejidos dentro de metodos vanilla existentes. No confundir esta atribucion de pertenencia con el orden secuencial completo: Pilar 1b (hook ASM sobre `MixinApplicatorStandard.apply(SortedSet<MixinInfo>)`) sigue pendiente y es el unico que puede justificar subir `orderTrust`/`runtimeMixinOrderTrusted`. Nota de arquitectura futura (no implementada, solo documentada): considerar `LiveClassModel` como representacion canonica de una clase capturada en runtime (`summary`/`members`/`annotations`/`mixinMerged`/`sha256`) antes de que Pilares 2-4 sumen mas recorridos ASM independientes sobre los mismos bytes vivos — el objetivo no es rendimiento sino que `ClassFile` deje de ser un parser reinventado por cada Pilar y pase a ser la fuente unica de verdad estructural de una clase.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar T3 corte I (Pilar 0): snapshot exacto del bytecode vivo transformado por Mixin, reemplazando el intento fallido de `retransformClasses` (corte H) por captura pasiva en la primera definicion real.
Intent: Instalar un `ClassFileTransformer` puramente observacional (`Tier3LiveCapture`) en `Agent.premain`, misma ventana que `LedgerTransformer` (ya probada contra clases Minecraft reales). `Boot.run` calcula un watch-set global con los targets `@Mixin` de todos los mods tier>=3 justo tras enumerar mods/tiers, antes de `waitForGameInstance`. El transformer cachea con `putIfAbsent` la primera definicion de cada target (ya tejida por Mixin si Fabric/Forge la transformo antes del `defineClass` nativo). Wiring del resultado en `Tier3MixinAudit`: nuevos campos `liveBytecode`/`liveBytesCaptured*`, diff real (SHA256) contra la base reusando el motor de diff de corte C, y `buildTxGate` deja de hardcodear `runtimeTransformedBytesCaptured=false` para reflejar el agregado real. `canLowerDecision` se mantiene `false` a proposito: Pilares 1-4 (orden runtime real, quiescencia, aplicador tx, rollback real) quedan fuera de alcance.
Updated: 2026-07-03 09:10
Current intent: N/A, corte cerrado en una sola sesion.
Completed: 2026-07-03 09:10
Result: T3 corte I cerrado. `Tier3LiveCapture` (nuevo) instalado en premain captura pasivamente bytecode vivo; `Boot.computeTier3WatchSet` (nuevo) alimenta el watch-set global reusando `Tier3MixinAudit.scanMixins/scanTargets` (extraidos de `plan()` sin duplicar parseo, tambien usados por el audit on-demand `t3.mixinPlan`). Runtime verificado: captura parcial honesta 36/41 targets para `balm` (`liveBytesCaptured=false`, agregado real, no forzado a 100%); 41 diffs con base+vivo comparables, los 41 confirman `liveSha256 != baseSha256` (transformacion real capturada, no relectura de base). `txGate.runtimeTransformedBytesCaptured` refleja ese agregado; blockers `demix_not_executable`/`tier3_tx_apply_not_implemented`/`rollback_pipeline_pending` siguen abiertos a proposito. Control negativo intacto: `tx.disablePlan` sigue `requires_restart`/`tier3_demix_not_viable`, `canLowerDecision=false`, sin veto ni runtime refs mutadas.
Files touched: `agent/src/dev/mksa/agent/Tier3LiveCapture.java` (nuevo), `agent/src/dev/mksa/agent/Agent.java`, `agent/src/dev/mksa/agent/Tier3MixinAudit.java`, `agent/src/dev/mksa/agent/Boot.java`, `agent/src/dev/mksa/agent/Tier3RetransformCaptureSmoke.java` (solo javadoc), `agent/tmp-build/rebuild.ps1` (nuevo, script de rebuild nativo PowerShell reconstruido desde cero), `launcher/src-tauri/src/bin/verify-fase1.rs`, `docs/proyecto.md` (§3.30 nuevos bullets), `docs/log.txt`.
Command/check expected: `javac --release 8` agent, rebuild nativo PowerShell de `agent/dist/fable-agent.jar` (con `Tier3LiveCapture.class`/`Tier3MixinAudit.class` incluidos), `cargo check`/`cargo build --bin verify-fase1`, runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados y salida `tier3-i-smoke.txt`.
Expected success signal: `liveBytesCapturedCount>0`, al menos una muestra con `liveSha256 != baseSha256`, `txGate.runtimeTransformedBytesCaptured` refleja el agregado real, `blockers` ya no incluye `runtime_transformed_bytes_missing` cuando el agregado da `true` (no aplico en este run: cobertura parcial 36/41, agregado `false`, blocker presente honestamente), `canLowerDecision` sigue `false`, `tx.disable` sigue fallando con `TIER3_DEMIX_NOT_EXECUTABLE`.
Verification:
- OK `javac --release 8` agent (solo warnings Java 8 obsoleto/unchecked conocidos).
- OK rebuild nativo PowerShell de `agent/dist/fable-agent.jar` (15189192 bytes): bridge/ledger/sqlite anidados, ASM solo dentro de `mksa-ledger.jar`, sin clases bridge/ledger/ASM sueltas, `Tier3MixinAudit.class` y `Tier3LiveCapture.class` presentes. Bug encontrado y corregido en el script: la extraccion de jars ASM referenciaba `..\$($_.Name)` en vez de `..\lib\$($_.Name)` (fallaba con `NoSuchFileException`).
- OK `cargo check --bin verify-fase1` y `cargo build --bin verify-fase1` (solo 5 warnings preexistentes de `id` no leido, ninguno en el codigo tocado).
- OK runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados, salida `tier3-i-smoke.txt`: candidato `balm`, `bytecodeCapturado=36/41 missing=5 liveBytesCaptured=false`; `diffs con bytes vivos: available=41 confirmed_differs_from_base=41`; todas las aserciones nuevas de corte I pasaron (`✔`); `tx.disablePlan` conserva `requires_restart`/`tier3_demix_not_viable`; `txGate.canLowerDecision=false`; sin veto ni runtime refs mutadas. `VERIFICACION OK`, `EXIT_CODE=0`, cierre WM_CLOSE limpio (region files a salvo, sin `taskkill /F`).
Recovery notes: No forzar `liveBytesCaptured`/`canLowerDecision` a `true` artificialmente; la cobertura parcial (36/41) es un reporte honesto del mecanismo pasivo (algunos targets pueden no cargar durante la ventana del smoke). Si se necesita mas cobertura, ampliar el watch-set o el tiempo de espera antes de leer `t3.mixinPlan`, no simular captura. Pilares 1-4 (orden runtime real de Mixin, quiescencia de transformers, aplicador transaccional sobre targets Minecraft reales, rollback demostrado bajo fallo) siguen pendientes como proximos cortes.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar T3 corte H: pasar de smoke controlado a una ruta ejecutable segura para desactivar/restaurar Tier 3.
Intent: Revisar la compuerta actual (`txGate.canLowerDecision=false`, `TIER3_DEMIX_NOT_EXECUTABLE`) y agregar el siguiente primitivo real necesario sin romper clases Minecraft compartidas. El objetivo inmediato es habilitar una ruta transaccional solo cuando el plan pueda demostrar targets no compartidos, orden confiable, redefine disponible, rollback y receipt; si no, mantener bloqueo honesto. No forzar el boton Tier 3 ni redefinir targets compartidos.
Updated: 2026-07-03 06:20
Current intent: Inspeccionar `Tier3MixinAudit`, `Ledger.modThinDisable/Enable` y el harness para identificar una primera clase de Tier 3 ejecutable sin replay de co-mixins; luego implementar compuerta/aplicador acotado o documentar el blocker tecnico concreto si no existe tal clase en el pack.
Updated: 2026-07-03 07:05
Current intent: Corregir el smoke de corte H: `tier3-h-smoke.txt` muestra que el agente devuelve `txGate.runtimeTransformedBytesCaptured=false` y `bytecodeDiffReason=runtime_transformed_bytes_not_captured_exactly`, pero el harness lee `bytecodeDiffReason` desde el objeto equivocado. No hay JVM/Minecraft vivo; no hace falta accion manual dentro del mundo para esta prueba IPC.
Completed: 2026-07-03 07:40
Result: T3 corte H cerrado como frontera tecnica ejecutable/diagnostica. El agente expone `t3.retransformCaptureSmoke`, que instala un transformer acotado a `dev.mksa.agent.Tier3ControlledDemixTarget`, redefine la clase a variante base y fuerza `Instrumentation.retransformClasses`. El transformer captura bytes (`captureCount=1`, `capturedSha256` valido), pero la captura no coincide ni con el bytecode mixed redefinido ni con el recurso base puro; demuestra que retransform observa la salida del transformer y no entrega un snapshot exacto del bytecode vivo redefinido. Por eso `txGate.runtimeTransformedBytesCaptured=false`, `bytecodeDiffReason=runtime_transformed_bytes_not_captured_exactly` y Tier 3 real sigue bloqueado honestamente.
Files likely touched: `agent/src/dev/mksa/agent/Tier3MixinAudit.java`, posible helper `Tier3DemixTx*.java`, `agent/src/dev/mksa/agent/Ledger.java`, `launcher/src-tauri/src/bin/verify-fase1.rs`, docs/log/proyecto/session-handoff.
Command/check expected: `javac --release 8` agent, rebuild nativo PowerShell de `agent/dist/fable-agent.jar`, `cargo check --bin verify-fase1`, smoke runtime Tier 3 acotado con salida `tier3-h-smoke.txt`.
Expected success signal: La compuerta Tier 3 solo baja a `ask`/ejecutable para un caso probado y reversible; `tx.disable` emite receipt con apply/rollback y `tx.enable` restaura. Para mods con targets compartidos o replay no confiable, `requires_restart`/bloqueo sigue intacto con razones precisas.
Verification:
- OK `cargo check --bin verify-fase1` tras corregir el harness (solo warnings existentes de `id` no leido).
- OK runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados y salida `tier3-h-smoke.txt`: candidato `balm`, `txGate.runtimeTransformedBytesCaptured=false`, `bytecodeDiffReason=runtime_transformed_bytes_not_captured_exactly`, `t3.redefineSmoke` OK, `t3.controlledDemixSmoke` OK, `t3.retransformCaptureSmoke` OK con `captureCount=1`, `valueBeforeRetransform=baseOFF`, `valueAfterRetransform=baseOFF`, `finalRestored=true`, `capturedMatchesMixed=false`, `capturedMatchesBase=false`, `touchesMinecraftClasses=false`, `touchesMixinTargets=false`.
- OK el mismo smoke confirma que `DisablePlan` de `balm` conserva `requires_restart`, `tx.disable` falla cerrado con `TIER3_DEMIX_NOT_EXECUTABLE`, no arma veto y no desuscribe refs runtime. `tier3-h-smoke.txt` termina con `VERIFICACION OK`; `quiesce` agoto 300s por churn, pero WM_CLOSE guardo mundos y cerro limpio sin `taskkill /F`.
Recovery notes: No cambiar `canLowerDecision` globalmente. No redefinir targets Minecraft compartidos ni targets con co-mixins hasta tener replay real. Si no hay candidato no compartido seguro en el pack, cerrar el corte H como diagnostico de frontera y siguiente requisito tecnico, no como toggle falso. Si el runtime smoke vuelve a fallar en la asercion de snapshot, revisar que el harness lea `aok["bytecodeDiffReason"]`, no el sobre IPC externo.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar T3 corte G: primer demix transaccional ejecutable sobre clases controladas. Demostrar disable/restore hot Tier 3 con receipt y rollback sin tocar targets Minecraft.
Intent: Construir un pipeline de demix real pero acotado a una clase privada del agente: una clase target propia con efecto tipo "mixin aplicado", un aplicador que redefine a variante base/disabled, un restore que redefine a variante applied, receipt con pasos y rollback garantizado. Integrar un smoke IPC/harness que pruebe disable -> comportamiento base -> restore aplicado y rollback si falla. Mantener `DisablePlan` de mods Tier 3 reales bloqueado hasta que el pipeline soporte targets Minecraft/Mixin.
Updated: 2026-07-03 05:58
Current intent: Ejecutar runtime `--tier3-audit` con `t3.controlledDemixSmoke`: validar `mixedON -> baseOFF -> mixedON`, receipt `tier3_demix_tx_receipt_v1`, hashes distintos, finalRestored y que Tier 3 real (`balm`) sigue bloqueado.
Completed: 2026-07-03 06:12
Result: T3 corte G cerrado. El agente expone `t3.controlledDemixSmoke`, un demix transaccional controlado sobre `dev.mksa.agent.Tier3ControlledDemixTarget`, clase privada del agente. El smoke parte en variante mixed/applied (`mixedON`), redefine a variante base/disabled (`baseOFF`), redefine de vuelta a mixed/applied y emite receipt `tier3_demix_tx_receipt_v1` con pasos `capture_current`, `reset_target_to_base` y `restore_mixin_variant`. El mecanismo queda acotado: no toca clases Minecraft ni targets Mixin reales. `DisablePlan`/`tx.disable` para Tier 3 real siguen bloqueados hasta que exista demix ejecutable sobre pipeline Mixin real.
Files likely touched: `agent/src/dev/mksa/agent/Tier3ControlledDemix*.java` (nuevos), `agent/src/dev/mksa/agent/Agent.java`, `agent/src/dev/mksa/agent/Ledger.java` o helper T3, `launcher/src-tauri/src/bin/verify-fase1.rs`, docs/log/proyecto/session-handoff.
Command/check expected: `javac --release 8` agent, rebuild nativo PowerShell de `agent/dist/fable-agent.jar`, `cargo check --bin verify-fase1`, runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados y salida `tier3-g-smoke.txt`.
Expected success signal: IPC nuevo o extendido ejecuta demix controlado: `preValue=applied`, disable redefine a `base`, restore redefine a `applied`, `receipt.status=OK`, pasos con hashes/rollback, `finalRestored=true`; si una verificacion falla, rollback deja la clase en estado aplicado. `tx.disablePlan` de `balm` sigue `requires_restart` y `tx.disable` sigue bloqueado para Tier 3 real.
Verification:
- OK runtime `cargo run --bin verify-fase1 -- --tier3-audit` desde `launcher/src-tauri` con permisos escalados y salida `tier3-g-smoke.txt`: candidato `balm`, `t3.controlledDemixSmoke` devolvio `status=ok`, `preValue=mixedON`, `disableValue=baseOFF`, `restoreValue=mixedON`, `finalValue=mixedON`, `finalRestored=true`, `redefineSupported=true`, `modifiableClass=true`, `receiptModel=tier3_demix_tx_receipt_v1`, `receipt.status=OK`, hashes mixed/base distintos, `targetScope=agent_private_test_class`, `touchesMinecraftClasses=false`, `touchesMixinTargets=false`.
- OK el mismo smoke confirma que `tx.disablePlan` de `balm` conserva `decision=requires_restart` por `tier3_demix_not_viable` y que `tx.disable` falla cerrado con `TIER3_DEMIX_NOT_EXECUTABLE`, devolviendo el gate Tier 3 sin armar veto ni desuscribir refs runtime.
- OK `tier3-g-smoke.txt` termina con `VERIFICACION OK` y veredicto: "Demix transaccional controlado ejecuta mixed->base->mixed con receipt/rollback; Tier 3 real sigue bloqueado." `quiesce` agoto 300s por churn, pero WM_CLOSE guardo mundos y cerro limpio; el wrapper elevado devolvio codigo 1 por la ruta conocida.
Recovery notes: No redefinir clases Minecraft ni targets Mixin reales en este corte. El objetivo es validar la transaccion y rollback de demix en clase propia antes de escalar a runtime Mixin real.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar T3 corte F: integracion con `DisablePlan`/`tx`. Pasar de `requires_restart` a `ask` o `auto` solo cuando el demix sea viable, con receipt y rollback.
Intent: Conectar la auditoria Tier 3 y el soporte de redefine controlado al plano de decision transaccional sin ejecutar demix real todavia. `DisablePlan` debe reportar una compuerta explicita de viabilidad (`safeToExecute`/`executable`/soporte redefine/base bytes/orden runtime) y conservar `requires_restart` mientras falte cualquiera. Si en el futuro el demix es viable, el plan debe poder bajar a `ask`/`auto` solo con rollback disponible y receipt de pasos.
Updated: 2026-07-03 05:28
Current intent: Ejecutar smoke runtime `--tier3-audit` con el nuevo contrato `t3TxGate`: validar que `DisablePlan` conserva `requires_restart` para demix no viable, embebe gate/receipt/rollback requerido, y que `tx.disable` bloquea Tier 3 con `TIER3_DEMIX_NOT_EXECUTABLE` sin mutar veto/runtime refs.
Completed: 2026-07-03 05:34
Result: T3 corte F cerrado. `Tier3MixinAudit` ahora emite `txGate` (`tier3_tx_gate_v1`) y `Ledger.modThinPlan` lo embebe como `t3TxGate` con `policyVersion=t3-corte-f-tx-gate`. `DisablePlan` solo puede bajar de `requires_restart` a `ask`/`auto` si `canLowerDecision=true`; hoy queda `false` porque el demix sigue diagnostico/no ejecutable, falta orden runtime real de Mixin, quiescencia de transformers, aplicador tx y rollback real. `tx.disable` queda protegido: para Tier 3 no viable falla cerrado con `TIER3_DEMIX_NOT_EXECUTABLE` y devuelve el gate, sin ejecutar Tier1/runtime por accidente.
Files likely touched: `agent/src/dev/mksa/agent/Ledger.java`, `agent/src/dev/mksa/agent/Tier3MixinAudit.java`, `launcher/src-tauri/src/bin/verify-fase1.rs`, docs/log/proyecto/session-handoff.
Command/check expected: `javac --release 8` backend agent, rebuild nativo PowerShell de `agent/dist/fable-agent.jar`, `cargo check --bin verify-fase1`, runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados y salida `tier3-f-smoke.txt`.
Expected success signal: Para `balm`, `tx.disablePlan` conserva `requires_restart` porque `demixPlan.safeToExecute=false`, pero ahora incluye gate/receipt de T3 con razones precisas y rollback requerido. El harness debe validar que no se baja a `ask/auto` si el demix no es ejecutable y que no muta veto/runtime refs.
Verification:
- OK `javac --release 8` agent (solo warnings Java 8 obsoleto/unchecked).
- OK `cargo fmt` y `cargo check --bin verify-fase1` (solo warnings existentes de `id` no leido).
- OK rebuild nativo PowerShell de `agent/dist/fable-agent.jar`: 14607047 bytes, bridge/ledger/sqlite anidados, ASM solo en `mksa-ledger.jar`, sin clases bridge/ledger/ASM sueltas; `Tier3MixinAudit` y `Tier3RedefSmoke` incluidos.
- OK runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados y salida `tier3-f-smoke.txt`: `balm`, 47 mixins, 41 targets, `txGate.canLowerDecision=false`, `redefineSupported=true`, `baseBytesCaptured=true`, `rollbackMechanismAvailable=true`, blockers `demix_not_executable`, `runtime_mixin_order_unverified`, `transformer_quiescence_unverified`, `tier3_tx_apply_not_implemented`, `rollback_pipeline_pending`; `DisablePlan decision=requires_restart`, razon `tier3_demix_not_viable` con `legacyCode=tier3_no_hot_pipeline`, `rollbackAvailable=false`; `tx.disable` falla cerrado con `TIER3_DEMIX_NOT_EXECUTABLE`; veto/runtimeDisabledRefs no cambian; `VERIFICACION OK` al final. `quiesce` agoto 300s por churn, pero WM_CLOSE guardo mundos y cerro limpio; wrapper elevado devolvio codigo 1 por ruta conocida.
Recovery notes: No ejecutar `Instrumentation.redefineClasses` sobre clases Minecraft ni targets Mixin en este corte. Este corte es integracion de decision/receipt; la unica redefinicion real permitida sigue siendo `t3.redefineSmoke`.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar T3 corte E: redefinicion hot controlada. Primer smoke con una clase target pequena: redefinir, verificar comportamiento y rollback si falla.
Intent: Agregar una superficie diagnostica controlada para probar `Instrumentation.redefineClasses` sobre una clase auxiliar pequena y propia, no sobre clases Minecraft/mixins reales. La prueba debe demostrar precheck de capacidades, snapshot de bytes originales, redefinicion reversible, verificacion de comportamiento y rollback garantizado.
Updated: 2026-07-03 05:00
Current intent: Regenerar `agent/dist/fable-agent.jar` con `Tier3RedefSmoke` y `Tier3RedefSmokeTarget` incluidos, preservando bridge/ledger/sqlite como jars anidados y sin clases ASM/bridge/ledger sueltas en el agent jar.
Updated: 2026-07-03 05:07
Current intent: Ejecutar `cargo run --bin verify-fase1 -- --tier3-audit` con el jar regenerado para validar `t3.redefineSmoke` en runtime: soporte de redefine, transicion original -> patched -> original, rollback y `DisablePlan` aun bloqueando mixins reales.
Completed: 2026-07-03 05:14
Result: T3 corte E cerrado. El agente expone `t3.redefineSmoke`, que prueba `Instrumentation.redefineClasses` contra `dev.mksa.agent.Tier3RedefSmokeTarget`, una clase privada pequena del agente. El smoke valida soporte/modificabilidad, captura bytes originales, parchea solo el literal `original` -> `patched!` sin cambiar schema, verifica comportamiento, hace rollback a los bytes originales y deja `finalRestored=true`. La auditoria Tier 3 sigue sin redefinir clases Minecraft ni targets Mixin reales; `DisablePlan` permanece en `requires_restart` con `tier3_no_hot_pipeline`.
Files likely touched: `agent/src/dev/mksa/agent/Tier3RedefSmokeTarget.java` (nuevo), `agent/src/dev/mksa/agent/Tier3RedefSmoke.java` (nuevo o integrado), `agent/src/dev/mksa/agent/Ledger.java`, `launcher/src-tauri/src/bin/verify-fase1.rs`, docs/log/proyecto/session-handoff.
Command/check expected: `javac --release 8` backend agent, rebuild nativo PowerShell de `agent/dist/fable-agent.jar`, `cargo check --bin verify-fase1`, runtime smoke nuevo o `--tier3-audit` extendido con permisos escalados.
Expected success signal: IPC devuelve preValue/original -> patchedValue -> rollbackValue original, `rollbackOk=true`, `redefineSupported=true`, `targetClass` propia pequena, y no toca clases Minecraft ni pipeline Mixin real. Si falla la redefinicion, rollback debe intentarse y el resultado debe reportar fallo sin dejar estado parcheado.
Verification:
- OK rebuild nativo PowerShell de `agent/dist/fable-agent.jar`: bridge/ledger/sqlite anidados, ASM solo dentro de `mksa-ledger.jar`, sin clases `dev/mksa/bridge`, `dev/mksa/agent/ledger` ni `org/objectweb/asm` sueltas en el agent jar; `Tier3RedefSmoke.class` y `Tier3RedefSmokeTarget.class` incluidos. Artefacto: 14604709 bytes, 2026-07-03 05:06:41.
- OK `cargo check --bin verify-fase1` (solo warnings existentes de `id` no leido).
- OK runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados y salida `tier3-e-smoke.txt`: candidato `balm`, `t3.redefineSmoke` devolvio `status=ok`, `redefineSupported=true`, `modifiableClass=true`, `preValue=original`, `patchedValue=patched!`, `rollbackValue=original`, `rollbackOk=true`, `finalRestored=true`, hashes original/parcheado distintos, `targetScope=agent_private_test_class`, `touchesMinecraftClasses=false`, `touchesMixinTargets=false`. `DisablePlan` conserva `requires_restart`/`tier3_no_hot_pipeline`; `VERIFICACION OK` al final. `quiesce` agoto 300s por churn, pero WM_CLOSE guardo mundos y cerro limpio; el wrapper elevado devolvio codigo 1 por la ruta conocida.
Recovery notes: no redefinir targets Minecraft todavia. Evitar cambios de schema de clase: mismo nombre/campos/metodos/constant pool compatible; solo cambiar cuerpo/constante de metodo en clase propia. Si `Instrumentation.isRedefineClassesSupported()` es false, reportar `unsupported` y no fallar el juego.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar T3 corte D: plan de demix. La auditoria debe calcular un orden seguro para quitar/reaplicar transformaciones sin romper otros mods que tambien tocaron la misma clase.
Intent: Extender `Tier3MixinAudit` con un plan diagnostico de demix por target: agrupar mixins por clase objetivo, identificar co-transformadores, calcular orden de replay conservador y marcar riesgos/conflictos sin ejecutar redefinicion ni re-aplicar Mixin.
Completed: 2026-07-03 04:35
Result: T3 corte D cerrado. `Tier3MixinAudit` construye un indice global de mixins desde `rootsByNs`, cruza los targets de la victima contra otros namespaces y emite `demixPlan` (`target_replay_plan_v1`). Cada target plan incluye base capturada, si esta cargado, co-owners, mixins victima a retirar, mixins preservados a re-aplicar, pasos `capture_base -> reset_target_to_base -> replay_mixin`, requisitos de ejecucion real y riesgos (`shared_target`, `runtime_order_unverified`, `preserved_mixin_deep_transform`). El plan queda `safeToExecute=false`/`executable=false` hasta tener orden runtime real y pipeline de redefinicion.
Files likely touched: `agent/src/dev/mksa/agent/Tier3MixinAudit.java`, `launcher/src-tauri/src/bin/verify-fase1.rs`, docs/log/proyecto/session-handoff.
Command/check expected: `javac --release 8` backend agent, rebuild nativo PowerShell de `agent/dist/fable-agent.jar`, `cargo check --bin verify-fase1`, runtime `verify-fase1.exe --tier3-audit` con permisos escalados y salida redirigida si hace falta.
Expected success signal: `t3.mixinPlan` conserva corte C y agrega plan de demix con targets, victim mixins, co-mixins/co-owners, orden remove/replay, `safeToExecute=false` hasta pipeline real, riesgos explicitos para targets compartidos, y `DisablePlan` sigue `requires_restart` sin mutar veto/runtime refs.
Verification:
- OK `javac --release 8` backend agent (solo warnings Java 8 obsoleto/unchecked).
- OK rebuild nativo PowerShell de `agent/dist/fable-agent.jar` (bridge/ledger/sqlite anidados, ASM solo en `mksa-ledger.jar`).
- OK `cargo check --bin verify-fase1` tras `cargo fmt` (solo warnings existentes de `id` no leido).
- OK runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados y salida `tier3-d-smoke.txt`: eligio `balm`, 47 mixins, 41 targets, `baseBytesCaptured=true`, `demixPlanTargetsTotal=41`, `demixPlanSharedTargetsTotal=26`, `coMixinTargetsTotal=86`, muestras con co-owners reales (`fabric-content-registries-v0`, `fabric-events-interaction-v0`, etc.), pasos y riesgos; `DisablePlan` preservo `requires_restart`, veto/runtimeDisabledRefs sin cambios. `quiesce` agoto 300s por churn, WM_CLOSE guardo y cerro limpio; el log contiene `VERIFICACION OK` aunque el wrapper elevado devolvio codigo 1.
Recovery notes: no usar `Instrumentation.redefineClasses`, no intentar desactivar mixins reales ni inferir seguridad completa. Si no se puede conocer orden global de Mixin, reportar `orderTrust=declaration_order`/riesgo conservador; el corte D es planificacion diagnostica. Si el smoke elevado devuelve codigo 1 sin stdout, revisar `tier3-d-smoke.txt`: puede contener `VERIFICACION OK` con cierre limpio.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar T3 corte C: modelo de atribucion/diff para mixins sin redefinir clases. La auditoria debe decir que mixin/mod toca que target y reportar un diff estructural inicial del bytecode aplicado contra base capturada.
Intent: Extender `Tier3MixinAudit` para construir atribuciones por mixin/target y comparar bytecode base vs bytecode cargado/recurso transformado de forma diagnostica, sin demix ni `Instrumentation.redefineClasses`.
Completed: 2026-07-03 04:10
Result: T3 corte C cerrado. `Tier3MixinAudit` conserva metadata de config por mixin, parsea miembros/anotaciones del classfile y expone `mixinAttributions` + `mixinDiffs` como modelo diagnostico `declaration_structural_v1`/`mixin_declaration_vs_base_v1`: cada muestra vincula `mixin -> target`, configs, operaciones (`method_transform`, `method_accessor_bridge`, `field_shadow_reference`, etc.), miembro exacto (`kind/name/desc`) y base capturada (`baseSize`/`baseSha256`). Sigue sin demix real ni `Instrumentation.redefineClasses`; `bytecodeDiffAvailable=false` hasta capturar bytes transformados reales.
Files likely touched: `agent/src/dev/mksa/agent/Tier3MixinAudit.java`, `launcher/src-tauri/src/bin/verify-fase1.rs`, docs/log/proyecto/session-handoff.
Command/check expected: `javac --release 8` backend bridge/ledger/agent, rebuild nativo PowerShell de `agent/dist/fable-agent.jar` si el runtime smoke debe usarlo, `cargo check --bin verify-fase1`, runtime `verify-fase1.exe --tier3-audit` con permisos escalados y salida redirigida si hace falta.
Expected success signal: `t3.mixinPlan` para `balm` conserva `baseBytesCaptured=true` y agrega atribucion/diff: samples que vinculan `mixin -> target`, totales coherentes, hash/tamano base vs aplicado cuando se pueda leer el bytecode cargado, y `DisablePlan` sigue `requires_restart` sin mutar veto/runtime refs.
Verification:
- OK `javac --release 8` backend agent sin bridge/ledger sueltos (solo warnings Java 8 obsoleto/unchecked).
- OK rebuild nativo PowerShell de `agent/dist/fable-agent.jar` (bridge/ledger/sqlite anidados, ASM solo en `mksa-ledger.jar`).
- OK `cargo check --bin verify-fase1` tras `cargo fmt` (solo warnings existentes de `id` no leido).
- OK runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados y salida `tier3-c-smoke.txt`: eligio `balm`, 47 mixins, 41 targets, `baseBytesCaptured=true`, `mixinAttributionsTotal>=47`, `mixinDiffsTotal=47`, samples con `mixin`, `target`, `configs`, `operationCounts`, `changes` con `operation/kind/name/desc/baseMemberPresent`, y `DisablePlan` preservo `requires_restart`, veto/runtimeDisabledRefs sin cambios. `quiesce` agoto 300s por churn, pero WM_CLOSE guardo y cerro limpio; el log contiene `VERIFICACION OK` aunque el wrapper elevado devolvio codigo 1.
Recovery notes: no intentar demix real ni redefinir clases. Si no se puede obtener bytes aplicados de alguna clase cargada, reportar diff parcial con razon explicita, no marcar hot eligible. Mantener fallback classloader de corte B. Si el smoke elevado devuelve codigo 1 sin stdout, revisar `tier3-c-smoke.txt`: puede haber `VERIFICACION OK` con cierre limpio pese al exit code del wrapper.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar T3 corte B: captura de bytecode base para targets mixin, sin redefinir clases. La auditoria debe reportar bytes base capturados por recurso classpath, hash/tamano/fuente y mantener `DisablePlan` en `requires_restart` hasta que exista pipeline demix.
Updated: 2026-07-03 02:31
Intent: Regenerar artefactos del agente para que el smoke use la captura base actual de `Tier3MixinAudit`, luego ejecutar verificacion estatica y runtime `--tier3-audit`.
Completed: 2026-07-03 03:28
Result: T3 corte B cerrado. `Tier3MixinAudit` captura bytecode base por recurso de classpath para todos los targets mixin auditados, reporta `size`/`sha256`/`source` por muestra y conserva `baseBytecodeTrust=classpath_resource_predefine`. El plan sigue sin mutacion hot: `hotEligible=false`, `tx.disablePlan` queda en `decision=requires_restart` por `tier3_no_hot_pipeline`, con `t3MixinPlan` embebido y sin tocar veto/runtime refs.
Files likely touched: `agent/src/dev/mksa/agent/Tier3MixinAudit.java`, `agent/src/dev/mksa/agent/Ledger.java`, `agent/src/dev/mksa/agent/Boot.java`, `launcher/src-tauri/src/bin/verify-fase1.rs`, docs/log/proyecto/session-handoff.
Command/check expected: rebuild nativo PowerShell de `agent/dist/fable-agent.jar`, `cargo check --bin verify-fase1`, `javac --release 8` backend bridge/ledger/agent, runtime `cargo run --bin verify-fase1 -- --tier3-audit`.
Verification:
- OK rebuild nativo PowerShell de `agent/dist/fable-agent.jar` (bridge/ledger anidados, sin ASM/bridge/ledger sueltos en agent jar).
- OK `javac --release 8` backend bridge/ledger/agent (solo warnings Java 8 obsoleto/unchecked).
- OK `cargo check --bin verify-fase1` (solo warnings existentes de `id` no leido).
- OK runtime `verify-fase1.exe --tier3-audit` con permisos escalados por assets/AppData y salida redirigida: eligio `balm`, audito 47 mixins y 41 targets, `baseBytesCaptured=true`, `baseBytesCapturedCount=41`, `baseBytesMissingCount=0`, muestras con `sha256`/`size`/`source`; `DisablePlan` preservo `requires_restart`, `tier3_no_hot_pipeline`, `baseBytesCaptured=true`; veto/runtimeDisabledRefs sin cambios. `quiesce` agoto 300s por churn, pero WM_CLOSE cerro limpio.
Recovery notes: no usar `Instrumentation.redefineClasses` todavia. Si futuras capturas no cubren todos los targets, reportar `baseBytesCaptured=false` con `base_bytecode_missing`; no fingir pre-mixin confiable. Si el runtime falla por assets 29, repetir con permisos escalados por Prism/AppData. El smoke elevado desde Codex/API puede no devolver stdout directo; redirigir a archivo del workspace para diagnostico.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-03
Objective: Implementar el primer corte Tier 3: diagnostico `t3.mixinPlan(ns)`/`modThinT3MixinPlan(ns)`, auditoria de mixins/coremods y enriquecimiento de `DisablePlan` sin habilitar mutacion Tier 3.
Completed: 2026-07-03 01:52
Result: Primer corte Tier 3 cerrado. El backend ya expone `t3.mixinPlan(ns)`/`modThinT3MixinPlan(ns)` como diagnostico puro de mixins/coremods; `tx.disablePlan(ns)` embebe `t3MixinPlan` para Tier 3 y devuelve `decision=requires_restart` con razon `tier3_no_hot_pipeline`, sin habilitar mutacion hot ni botones Tier 3.
Files touched: `agent/src/dev/mksa/agent/Tier3MixinAudit.java`, `agent/src/dev/mksa/agent/Ledger.java`, `agent/src/dev/mksa/agent/Agent.java`, `agent/src/dev/mksa/bridge/Bridge.java`, `agent/src/dev/mksa/bridge/LedgerSink.java`, `mod-thin/src/main/java/dev/mksa/modthin/BridgeProxy.java`, `launcher/src-tauri/src/bin/verify-fase1.rs`, docs.
Verification:
- OK `cargo check --bin verify-fase1` (solo warnings existentes de `id` no leido).
- OK `javac --release 8` backend bridge/ledger/agent con directorios temporales propios (solo warnings Java 8 obsoleto/unchecked).
- OK `javac --release 21` mod-thin con `mod-thin/lib/*`.
- OK runtime `cargo run --bin verify-fase1 -- --tier3-audit` con permisos escalados por assets/AppData: eligio `balm`, audito 47 mixins y 41 targets, `hotEligible=false`, `baseBytesCaptured=false`, `DisablePlan decision=requires_restart`, razon `tier3_no_hot_pipeline`, `t3MixinPlan` embebido, veto/runtimeDisabledRefs sin cambios. Cierre final por WM_CLOSE limpio; `quiesce` agoto 300s por churn antes de cerrar.
Recovery notes: no activar ejecucion Tier 3 ni redefinir clases desde UI. Si un runtime smoke falla con "No se encontro el indice de assets 29" desde Codex/API, repetir con permisos escalados por acceso a Prism/AppData. `verify-fase1.rs` fue pasado por `rustfmt`; hay churn de formato en ese archivo junto con los cambios funcionales.

## End Active Task

## Active Task
Status: completed
Started: 2026-07-02 23:29
Objective: Modificar el icono `loot_tables` para que coincida exactamente con la imagen de referencia del cofre provista por el usuario.
Completed: 2026-07-02 23:31
Result: Icono `loot_tables.png` en `minecraft_icons` actualizado con éxito. Ahora tiene las proporciones exactas de 8x8 píxeles del cofre de referencia provisto por el usuario, centrado en el lienzo de 16x16, incluyendo la paleta de colores de madera degradada y el cerrojo blanco/gris tridimensional.

## End Active Task
Files likely touched:
- agent/src/dev/mksa/agent/ModSemantics.java
- mod-thin/src/main/java/dev/mksa/modthin/BridgeProxy.java
- mod-thin/src/main/java/dev/mksa/modthin/ModsScreen.java
- docs/modelo-de-datos.md
- docs/proyecto.md
- docs/resumen-proyecto.md
- docs/log.txt
- docs/session-handoff.md
Command/check expected:
- javac --release 8 backend no-bridge/no-ledger con cp agent/out-bridge.
- javac --release 21 mod-thin con cp mod-thin/lib/*.
Verification:
- OK `javac --release 8 -cp agent/out-bridge -d agent/out-check ...` (solo warnings de source/target 8 obsoletos y unchecked).
- OK `javac --release 21 -cp "mod-thin/lib/*" -d mod-thin/out-check ...`.
- Directorios temporales `agent/out-check` y `mod-thin/out-check` eliminados despues de compilar.
Recovery notes:
- El repo ya trae muchos cambios sin commit. No revertir cambios ajenos. Si `wcg.refs` no esta listo, la UI debe mostrar impacto como pendiente/conservador, no bloquear la lista.
- Si la compilacion falla, revisar primero firmas entre `ModSemantics`, `BridgeProxy.SemanticProfile` y `ModsScreen` antes de cambiar comportamiento de disable.
- Para la preparacion del jar, tocar solo `mod-thin/out`, `mod-thin/dist`, `mod-thin/out-tools`, el sprite generado y `Instancia 3/minecraft/mods/mksa-thin.jar`.
Follow-up in_progress: restaurar el panel `Informacion del mod` en `ModsScreen` sin reintroducir el ajuste de filas del listado. Dejar el listado como antes, recompilar `mod-thin` y copiar el jar a `Instancia 3/minecraft/mods/mksa-thin.jar`.
Follow-up completed: restaurado el panel `Informacion del mod` en `ModsScreen` sin tocar el ajuste de filas del listado. `mod-thin/dist/mksa-thin.jar` fue recompilado y copiado a `Instancia 3/minecraft/mods/mksa-thin.jar` (375915 bytes, 2026-07-02 14:01:18).
Follow-up completed: `mod-thin/src/main/java/dev/mksa/modthin/ModsScreen.java` restaurado desde el repo a una copia limpia compilable y `mod-thin/dist/mksa-thin.jar` regenerado/copied a `Instancia 3/minecraft/mods/mksa-thin.jar` (370290 bytes, 2026-07-02 14:24:09) para que `cargo run --bin verify-fase1 -- --launch` arranque con una UI funcional.
Follow-up completed: aplicado el layout pedido en `ModsScreen` sin tocar otros archivos de UI. La pantalla quedo dividida en dos zonas simetricas; la lista renderiza 4 filas fijas; la barra de busqueda se recorto para dejar espacio a reload y tiers; se quitaron los botones subir/bajar; `Volver` quedo centrado; `Desactivar` y `Restaurar` quedaron alineados con la ultima fila; y el panel derecho `Informacion del mod` vuelve a mostrar icono + nombre + version. `mod-thin` fue recompilado con Java 21 y `mksa-thin.jar` se copio a `Instancia 3/minecraft/mods/mksa-thin.jar`.
Follow-up completed: corregido el solapamiento en `ModsScreen`. La caja negra de `Informacion del mod` ahora termina por encima de `Desactivar/Restaurar`, y la zona de la lista se ensancho unos pocos pixeles a la derecha para respirar mejor. `mod-thin` fue recompilado con Java 21 y `mksa-thin.jar` se copio otra vez a `Instancia 3/minecraft/mods/mksa-thin.jar`.
Follow-up completed: corregida la mala colocacion de `Desactivar` y `Restaurar` vista en `uie.png`. Los botones quedaron compactos en una sola fila debajo del panel `Informacion del mod`, sin fondo extra de acciones. `mod-thin` compilo con Java 21 y `mksa-thin.jar` fue copiado a `Instancia 3/minecraft/mods/mksa-thin.jar` (370669 bytes, 2026-07-02 14:53:32).
## End Active Task

## Active Task
Status: completed
Started: 2026-07-06 21:10
Objective: Corregir el NullPointerException post-enable descubierto en el smoke `--tier3-chatheads` (corte AM): `rendererPools` (campo `@Unique` agregado a `class_11228`/GuiRenderer por el mixin de `fabric-rendering-v1`) vuelve `null` tras un ciclo `disableGroup`/`enableGroup`, porque el target estaba en modo RESET pese a tener 3 mixins co-dueños reales, y RESET borra las tres contribuciones sin que ningun constructor vuelva a correr sobre la instancia ya viva. Instruccion explicita del usuario: "si no existe una manera oficial de solucionarlo vamos a crearla, para eso estamos aca, se que podemos".
Completed: 2026-07-06 22:35
Result: Nuevo `Tier3InstanceLocator.java` (BFS reflexivo generico sobre el grafo de objetos, raiz `Boot.clientInstance`, acotado a `MAX_DEPTH=10`/`MAX_VISITED=50000`) verificado primero con un diagnostico de solo lectura (`t3.probeInstance` + `--tier3-chatheads-probe`, resultado real `found=true`/6ms) antes de construir el resto. `Tier3DemixApply.java` gano `PRESERVE_FIELDS` (registro declarativo, hoy solo `class_11228->rendererPools`), `FIELD_SNAPSHOT`, y `snapshotPreservedFields`/`restorePreservedFields` (no fatales, solo advertencias) llamados justo antes/despues del `redefineClasses` atomico en `disableGroup`/`enableGroup`, con `fieldsPreserved`/`fieldRestoreWarnings` nuevos en los receipts JSON. Bug propio autodetectado antes de compilar (classloader equivocado via `Class.forName`) y corregido pasando la `Class` ya resuelta por `resolveLoadedClass`. `bash agent/build.sh` y `cargo build --bin verify-fase1` limpios. El usuario corrio el smoke completo (`--tier3-chatheads`, 3 fases) el mismo tras el fix: cabeza desaparece sin crash al disable, reaparece sin crash al enable -- CONFIRMADO REAL, cero NullPointerException. Efecto cosmetico observado (mensajes de chat previos a un disable/enable no recuperan/pierden su cabeza retroactivamente) confirmado como contrato ya esperado, no un bug, documentado en el propio smoke test. Detalle completo en `docs/proyecto.md` §3.57, resumen en `docs/log.txt`.

## End Active Task
Files likely touched:
- agent/src/dev/mksa/agent/Tier3InstanceLocator.java (nuevo)
- agent/src/dev/mksa/agent/Tier3DemixApply.java
- agent/src/dev/mksa/agent/Agent.java
- launcher/src-tauri/src/bin/verify-fase1.rs
- docs/log.txt
- docs/proyecto.md
Command/check expected:
- `bash agent/build.sh` (javac --release 8, checks del agente).
- `cargo build --bin verify-fase1` en `launcher/src-tauri`.
Verification:
- OK `bash agent/build.sh` -- solo warnings preexistentes de source/target 8 obsoletos.
- OK `cargo build --bin verify-fase1` -- solo warnings preexistentes de reasignacion de `id`.
- OK `--tier3-chatheads-probe` contra Instancia 3: `t3.probeInstance` devolvio `found=true` en 6ms, confirmando que `class_11228` es alcanzable desde `Boot.clientInstance` antes de construir el fix completo.
- OK `--tier3-chatheads` (corrido por el usuario en su propia ventana, 3 fases con confirmacion visual real): cabeza desaparece sin crash al disable, reaparece sin crash al enable, `pid_alive` se mantuvo verdadero durante toda la Fase C.
Recovery notes:
- `PRESERVE_FIELDS` cubre unicamente `class_11228#rendererPools` (el campo empiricamente confirmado). `entity_model_features` y `supermartijn642corelib` agregan sus propios campos `@Unique` a la misma clase y no estan cubiertos -- si resurge un NPE similar en una ruta de codigo distinta, revisar primero si el campo afectado pertenece a alguno de esos dos mods y agregarlo al registro.
- `Tier3InstanceLocator.locate` requiere la `Class<?>` correcta (resuelta via `resolveLoadedClass`/`inst.getAllLoadedClasses()`, nunca `Class.forName` con el classloader del agente) -- Minecraft carga por el classloader Knot de Fabric.
- El efecto cosmetico de mensajes de chat con/sin cabeza "congelada" en el momento de renderizado es esperado, no revertir ni intentar corregir sin instruccion explicita del usuario.
